---
phase: 02-foundation
plan: 05
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/styles/theme.css
  - src/styles/components.css
  - src/styles/buttons.css
  - src/styles/icon-cube.css
  - src/styles/navigation.css
  - src/styles/pills.css
  - src/styles/inputs.css
  - src/styles/steps.css
  - src/styles/layout.css
  - src/styles/misc.css
  - src/styles/animations.css
  - src/styles/responsive.css
  - src/proxy/index.js
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04, CORE-09]
user_setup: []

must_haves:
  truths:
    - "13 CSS custom properties on :root match RECON.md §6.1 exactly — same names, same hex values"
    - "Only 4 properties are consumed via var(): --bg, --text, --muted, --border (matching original hardcoded pattern per Pitfall 5)"
    - ".btn-3d renders with 3-layer box-shadow creating 3D depth; :active moves down 4px with collapsed shadows"
    - ".btn-3d has 7 color variants (.success, .warn, .danger, .cyan, .purple, .ghost) + .sm size modifier"
    - ".icon-cube renders with 160deg diagonal gradient + 3-layer shadow; 5 color variants (.cyan, .green, .purple, .amber, .rose) + base indigo"
    - ".glass uses linear-gradient background + var(--border) + backdrop-filter:blur(10px)"
    - ".nav-link.active shows indigo→cyan diagonal gradient + inset box-shadow border"
    - "@media (max-width:1024px): sidebar becomes fixed overlay sliding in/out; backdrop appears"
    - "instalarProxy() monkey-patches window.fetch to rewrite Cloudflare/SMS24h URLs to /cf-api/ and /sms-api/"
    - "instalarProxy() skips on file: protocol (local development)"
  artifacts:
    - path: "src/styles/theme.css"
      provides: ":root CSS custom properties (13 variables), html/body base styles, font-display class"
      contains: ":root {"
      contains: "--bg: #0b1020"
      contains: "font-family: Inter"
    - path: "src/styles/components.css"
      provides: ".glass, .grad-card, .neon, .ring-glow, .grad-text — glassmorphism + glow effects"
      contains: "backdrop-filter: blur(10px)"
    - path: "src/styles/buttons.css"
      provides: ".btn-3d base + 7 color variants + .sm size + :hover/:active/:disabled states"
      contains: "translateY(4px)"
      contains: ".btn-3d.success"
    - path: "src/styles/icon-cube.css"
      provides: ".icon-cube base + 5 color variants (cyan, green, purple, amber, rose)"
      contains: "linear-gradient(160deg"
      contains: "inset 0 -6px 0"
    - path: "src/styles/navigation.css"
      provides: ".nav-link, .nav-link:hover, .nav-link.active, .nav-emoji, .sidebar transition"
      contains: ".nav-link.active"
    - path: "src/styles/pills.css"
      provides: ".pill base + 5 variants (.ok, .todo, .doing, .done, .danger)"
      contains: "border-radius: 999px"
    - path: "src/styles/inputs.css"
      provides: ".input, .input:focus, textarea.input, select.input with custom dropdown arrow"
      contains: "border-color: #6366f1"
    - path: "src/styles/steps.css"
      provides: ".step-card, .step-card.disabled, .step-card.done .step-num, .step-num"
      contains: "padding: 1.5rem 1.5rem 1.5rem 5rem"
    - path: "src/styles/layout.css"
      provides: "Header sticky/blur styles, content-wrap, sidebar static layout"
      contains: "backdrop-filter: blur(12px)"
    - path: "src/styles/misc.css"
      provides: ".copy-row, .empty, .scrollbar, .file-drop, .switch-tab, .pdf-canvas-wrap, .pdf-overlay-text, .spinner"
      contains: "::-webkit-scrollbar"
    - path: "src/styles/animations.css"
      provides: "@keyframes float, spin, pulse-ring + .floaty, .spinner, .pulse-ring classes"
      contains: "@keyframes float"
    - path: "src/styles/responsive.css"
      provides: "@media (max-width:1024px) — sidebar collapse, backdrop show, content-wrap margin reset"
      contains: "translateX(-100%)"
    - path: "src/proxy/index.js"
      provides: "instalarProxy() — window.fetch monkey-patch for CORS proxy"
      exports: ["instalarProxy"]
  key_links:
    - from: "src/styles/theme.css :root custom properties"
      to: "All CSS files + JS template strings"
      via: "4 properties consumed via var() in CSS; 9 properties serve as palette documentation (per Pitfall 5)"
    - from: "src/styles/responsive.css @media (max-width:1024px)"
      to: "src/router/index.js toggleSidebar()"
      via: "CSS hides/shows sidebar at breakpoint; JS toggles .open class for slide animation"
    - from: "src/proxy/index.js instalarProxy()"
      to: "_redirects (Plan 01)"
      via: "JS rewrites fetch URLs to /cf-api/* and /sms-api/*; Netlify _redirects forwards those to upstream APIs"
    - from: "index.html <link> tags"
      to: "All 12 src/styles/*.css files"
      via: "CSS loaded in dependency order: theme.css first (custom properties), component files, layout.css, responsive.css last"
---

<objective>
Create the complete CSS design system — all 13 :root custom properties, 23 component class families, 19 color variants, 4 keyframe animations, and responsive breakpoint at 1024px — exactly matching RECON.md §6.1-6.7. Also create the CORS proxy layer (instalarProxy()) monkey-patching window.fetch.

Purpose: The CSS design system is the visual identity layer — every widget, view, and layout element depends on it. The proxy layer enables API calls in production without CORS errors. Together they complete the Phase 02 Foundation: the app skeleton is fully styled and the networking layer is in place.

Output: 12 CSS files (~350 lines total) + 1 proxy JS module (~20 lines). After this plan, the app renders with the exact dark navy+indigo theme, 3D buttons, glassmorphism cards, and responsive mobile sidebar as the original.
</objective>

<execution_context>
@/home/matspectrum-ai/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/matspectrum-ai/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-foundation/02-CONTEXT.md
@.planning/phases/02-foundation/02-RESEARCH.md § Code Examples — CSS Custom Properties, Tailwind CDN Config
@.planning/phases/02-foundation/02-RESEARCH.md Pitfall 5 — CSS Hardcoded Values vs Custom Properties (CRITICAL)
@.planning/phases/02-foundation/02-RESEARCH.md § Pattern 5: CORS Proxy Layer
@.planning/phases/02-foundation/02-RESEARCH.md § Code Examples — instalarProxy()
@.planning/phases/01-reconnaissance-extraction/RECON.md §6.1-6.7 (complete CSS specification — custom properties, component classes, variants, animations, responsive breakpoints)
@.planning/phases/01-reconnaissance-extraction/RECON.md §5.10 instalarProxy() (lines 3001-3023)
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 02-05-01: Create Core CSS Design System Files (Theme, Components, Buttons, Icon Cubes)</name>
  <files>
    src/styles/theme.css
    src/styles/components.css
    src/styles/buttons.css
    src/styles/icon-cube.css
  </files>
  <read_first>
    - RECON.md §6.1 CSS Custom Properties — :root block with 13 variables (lines 3033-3053)
    - RECON.md §6.1.8 Typography — font-display, body fonts (lines 3115-3168)
    - RECON.md §6.2.1 Glassmorphism Cards — .glass, .grad-card, .neon, .ring-glow (lines 3224-3292)
    - RECON.md §6.2.2 3D Button System — .btn-3d base + 7 variants + states (lines 3294-3434)
    - RECON.md §6.2.3 Icon Cube Design System — .icon-cube base + 5 variants (lines 3441-3490)
    - RESEARCH.md Pitfall 5 — DO NOT convert hardcoded hex values to var() references (lines 581-589)
    - D-04 — CSS must match original pixel-identically
  </read_first>
  <action>
    Per D-04 and RESEARCH.md Pitfall 5, create 4 CSS files replicating the original's `<style>` block (RECON.md §6) EXACTLY — same properties, same values, same selectors.

    CRITICAL RULE (Pitfall 5): RECON.md documents that 9 of 13 `:root` custom properties are defined but NOT consumed via `var()`. Their hex values are hardcoded in CSS rules. Do NOT convert hardcoded hex to `var()` — that would change visual output. Only `--bg`, `--text`, `--muted`, `--border` are consumed via `var()`.

    FILE 1: src/styles/theme.css — CSS Custom Properties & Base

    ```css
    /* :root custom properties — matches RECON.md §6.1 lines 3037-3053 EXACTLY */
    :root {
      --bg: #0b1020;
      --bg2: #0f172a;
      --card: #111a36;
      --soft: #1a2348;
      --border: rgba(255,255,255,0.08);
      --text: #e6e9f5;
      --muted: #9aa3c7;
      --accent: #6366f1;
      --accent2: #22d3ee;
      --accent3: #a855f7;
      --ok: #22c55e;
      --warn: #f59e0b;
      --bad: #ef4444;
    }

    /* Base — only --bg, --text consumed via var() */
    html, body {
      background: var(--bg);
      color: var(--text);
      font-family: Inter, sans-serif;
    }

    /* Display font — Sora for headings */
    .font-display { font-family: Sora, sans-serif; }
    ```

    Properties that are NOT consumed via var() (hardcoded in component CSS): --bg2, --card, --soft, --accent, --accent2, --accent3, --ok, --warn, --bad. These serve as palette documentation.

    FILE 2: src/styles/components.css — Glassmorphism + Glow Effects

    Copy EXACTLY from RECON.md:

    .glass (line 3229-3234):
    - background: linear-gradient(145deg, rgba(255,255,255,0.04), rgba(255,255,255,0.01));
    - border: 1px solid var(--border);
    - backdrop-filter: blur(10px);

    .grad-card (line 3250-3253):
    - background: radial-gradient(120% 120% at 0% 0%, #1d275a 0%, #0e1430 60%, #0b1020 100%);

    .neon (line 3261-3263):
    - box-shadow: 0 0 0 1px rgba(99,102,241,.3), 0 0 40px -10px rgba(99,102,241,.5);

    .ring-glow + ::before (lines 3271-3287):
    - position:relative base + ::before with gradient border ring using mask technique
    - background: linear-gradient(120deg, #22d3ee, #a855f7, #22d3ee);
    - -webkit-mask + mask-composite for inner exclusion

    .grad-text (line 3920-3927):
    - background: linear-gradient(120deg, #a5b4fc, #22d3ee 50%, #a855f7);
    - -webkit-background-clip: text; background-clip: text; color: transparent;

    FILE 3: src/styles/buttons.css — 3D Button System

    Copy EXACTLY from RECON.md §6.2.2 (lines 3299-3433):

    .btn-3d base (3299-3319):
    - position:relative; display:inline-flex; align-items:center; justify-content:center; gap:0.6rem
    - padding:0.95rem 1.4rem; border-radius:16px; font-weight:700; font-size:0.98rem; letter-spacing:0.2px
    - color:white; background:linear-gradient(180deg, #6d72ff, #4f46e5);
    - box-shadow: 0 6px 0 #312e81, 0 12px 24px rgba(79,70,229,.45), inset 0 1px 0 rgba(255,255,255,.25);
    - transform:translateY(0); transition:transform 0.12s ease, box-shadow 0.12s ease, filter 0.12s ease;
    - user-select:none; cursor:pointer; border:none;

    :hover (3329-3330): filter:brightness(1.08);
    :active (3335-3343): transform:translateY(4px); box-shadow: 0 2px 0 #312e81, 0 6px 14px rgba(79,70,229,.4), inset 0 1px 0 rgba(255,255,255,.2);
    :disabled (3348-3354): opacity:0.45; cursor:not-allowed; filter:grayscale(0.3);

    7 COLOR VARIANTS — each overrides background + box-shadow:
    - .success (green gradient #34d399→#10b981, shadow #065f46)
    - .warn (amber gradient #fbbf24→#f59e0b, shadow #92400e, text color #1f1300)
    - .danger (red gradient #fb7185→#ef4444, shadow #7f1d1d)
    - .cyan (cyan gradient #67e8f9→#06b6d4, shadow #155e75, text #001017)
    - .purple (purple gradient #c084fc→#a855f7, shadow #581c87)
    - .ghost (dark gradient #1f2a52→#111a36, shadow #0a0f23, subtle inner highlight)

    .sm size modifier (3407-3418):
    - padding:0.55rem 0.85rem; font-size:0.82rem; border-radius:12px
    - box-shadow:0 4px 0 #312e81, inset 0 1px 0 rgba(255,255,255,.25);
    - :active: box-shadow:0 1px 0 #312e81, inset 0 1px 0 rgba(255,255,255,.2); transform:translateY(3px);

    Each variant also overrides :active box-shadow with matching darker color and reduced depth. See RECON.md for exact values.

    FILE 4: src/styles/icon-cube.css — Icon Cube Design System

    Copy EXACTLY from RECON.md §6.2.3 (lines 3446-3478):

    .icon-cube base (3446-3458):
    - width:64px; height:64px; border-radius:18px;
    - background:linear-gradient(160deg, #7c83ff, #4f46e5 60%, #312e81);
    - box-shadow: 0 10px 24px rgba(79,70,229,.45), inset 0 2px 0 rgba(255,255,255,.25), inset 0 -6px 0 rgba(0,0,0,.25);
    - display:flex; align-items:center; justify-content:center; font-size:28px; color:white;

    5 COLOR VARIANTS — each overrides background + box-shadow drop-shadow color:
    - .cyan: gradient #67e8f9→#06b6d4 60%→#155e75, shadow rgba(6,182,212,.45)
    - .green: gradient #34d399→#10b981 60%→#065f46, shadow rgba(16,185,129,.45)
    - .purple: gradient #c084fc→#a855f7 60%→#581c87, shadow rgba(168,85,247,.45)
    - .amber: gradient #fbbf24→#f59e0b 60%→#92400e, shadow rgba(245,158,11,.45)
    - .rose: gradient #fb7185→#ef4444 60%→#7f1d1d, shadow rgba(239,68,68,.45)

    Inner highlights (inset 0 2px 0 rgba(255,255,255,.25) and inset 0 -6px 0 rgba(0,0,0,.25)) are SHARED across all variants — only the gradient and ambient shadow color change.
  </action>
  <verify>
    <automated>python3 -c "
import os

# Verify all 4 files exist
for f in ['src/styles/theme.css','src/styles/components.css','src/styles/buttons.css','src/styles/icon-cube.css']:
    assert os.path.exists(f), f'Missing file: {f}'
    content = open(f).read()
    assert len(content) > 50, f'{f} is too small ({len(content)} chars)'

# theme.css checks
theme = open('src/styles/theme.css').read()
assert ':root' in theme, 'theme.css missing :root'
assert '--bg: #0b1020' in theme, 'theme.css missing --bg'
assert '--text: #e6e9f5' in theme, 'theme.css missing --text'
assert '--accent: #6366f1' in theme, 'theme.css missing --accent'
assert '--ok: #22c55e' in theme, 'theme.css missing --ok'
assert 'font-family: Inter' in theme, 'theme.css missing Inter font'
assert '.font-display' in theme, 'theme.css missing .font-display'

# components.css checks
comp = open('src/styles/components.css').read()
assert '.glass' in comp, 'components.css missing .glass'
assert 'backdrop-filter: blur(10px)' in comp, 'components.css missing blur'
assert '.grad-card' in comp, 'components.css missing .grad-card'
assert '.neon' in comp, 'components.css missing .neon'
assert '.grad-text' in comp, 'components.css missing .grad-text'

# buttons.css checks
btn = open('src/styles/buttons.css').read()
assert '.btn-3d' in btn, 'buttons.css missing .btn-3d'
assert 'translateY(4px)' in btn or 'translateY(3px)' in btn, 'buttons.css missing press animation'
assert '.btn-3d:hover' in btn or '.btn-3d:hover' in btn or 'btn-3d:hover' in btn, 'buttons.css missing hover'
assert '.success' in btn, 'buttons.css missing .success variant'
assert '.warn' in btn, 'buttons.css missing .warn variant'
assert '.danger' in btn, 'buttons.css missing .danger variant'
assert '.cyan' in btn, 'buttons.css missing .cyan variant'
assert '.purple' in btn, 'buttons.css missing .purple variant'
assert '.ghost' in btn, 'buttons.css missing .ghost variant'

# icon-cube.css checks
cube = open('src/styles/icon-cube.css').read()
assert '.icon-cube' in cube, 'icon-cube.css missing .icon-cube'
assert 'linear-gradient(160deg' in cube, 'icon-cube.css missing 160deg gradient'
assert 'inset 0 -6px 0' in cube, 'icon-cube.css missing bottom inner shadow'

# Verify hardcoded hex values are NOT replaced with var() (Pitfall 5)
# The accent colors should appear as hardcoded hex in component CSS, not var(--accent)
btns_css = open('src/styles/buttons.css').read()
# .btn-3d should have hardcoded #312e81, NOT var(--accent) or var(--something)
assert '#312e81' in btns_css or '312e81' in btns_css, 'buttons.css should use hardcoded shadow color'

print('ALL CORE CSS CHECKS PASSED')
"</automated>
  </verify>
  <acceptance_criteria>
    - 13 :root custom properties with exact hex values from RECON.md §6.1
    - Only --bg, --text, --muted, --border consumed via var() (Pitfall 5 compliance)
    - .glass: backdrop-filter blur, gradient background, var(--border)
    - .grad-card: radial gradient matching original
    - .neon: 2-layer indigo glow box-shadow
    - .grad-text: gradient text effect with background-clip
    - .btn-3d: 3-layer shadow (bottom + ambient + inner highlight)
    - .btn-3d:active: translateY(4px) + collapsed shadows
    - 7 btn-3d color variants (.success, .warn, .danger, .cyan, .purple, .ghost, .sm)
    - .icon-cube: 160deg diagonal gradient + 3-layer shadow
    - 5 icon-cube color variants (.cyan, .green, .purple, .amber, .rose)
    - All hardcoded hex values preserved (not converted to var())
  </acceptance_criteria>
  <done>Core CSS design system files created: theme custom properties, glassmorphism/glow effects, 3D button system with 8 variants, and icon cube system with 6 variants — all matching RECON.md §6.1-6.2 pixel-identically.</done>
</task>

<task type="auto">
  <name>Task 02-05-02: Create Navigation, Pills, Inputs, Steps, Layout, Misc, and Animations CSS</name>
  <files>
    src/styles/navigation.css
    src/styles/pills.css
    src/styles/inputs.css
    src/styles/steps.css
    src/styles/layout.css
    src/styles/misc.css
    src/styles/animations.css
  </files>
  <read_first>
    - RECON.md §6.2.4 Pill System (lines 3493-3524) — .pill base + 5 variants
    - RECON.md §6.2.5 Navigation Links (lines 3527-3589) — .nav-link + :hover + .active + .nav-emoji
    - RECON.md §6.2.6 Step Cards (lines 3592-3653) — .step-card + .disabled + .done .step-num + .step-num
    - RECON.md §6.2.7 Input Elements (lines 3656-3719) — .input + :focus + ::placeholder + textarea + select
    - RECON.md §6.2.8 Additional Classes (lines 3722-3942) — .switch-tab, .copy-row, .empty, .scrollbar, .file-drop, .pdf-canvas-wrap, .pdf-overlay-text
    - RECON.md §6.4 Animations (lines 3961-4044) — @keyframes float, spin, pulse-ring + classes
    - RECON.md §6.5 Sidebar + Backdrop (lines 3860-3891) — .sidebar transition, .backdrop
  </read_first>
  <action>
    Per D-04, create 7 CSS files replicating the original's component classes EXACTLY (RECON.md §6.2.4-6.2.8, §6.4).

    FILE 5: src/styles/navigation.css — Sidebar nav-links + emoji icon containers

    .nav-link (RECON.md line 3532-3542):
    - display:flex; align-items:center; gap:0.8rem; padding:0.85rem 1rem; border-radius:14px
    - color:#cfd5f2; font-weight:600; transition:all 0.15s ease; cursor:pointer

    .nav-link:hover (line 3549-3553):
    - background:rgba(255,255,255,0.05); color:white; transform:translateX(2px)

    .nav-link.active (line 3558-3563):
    - background:linear-gradient(135deg, rgba(99,102,241,.25), rgba(34,211,238,.1)); color:white
    - box-shadow:inset 0 0 0 1px rgba(255,255,255,.08)

    .nav-emoji (line 3572-3583):
    - width:36px; height:36px; display:inline-flex; align-items:center; justify-content:center
    - border-radius:10px; background:linear-gradient(160deg, rgba(255,255,255,.12), rgba(255,255,255,.04)); font-size:18px

    FILE 6: src/styles/pills.css — Status badges

    .pill (RECON.md line 3498-3507):
    - display:inline-flex; align-items:center; gap:0.4rem; padding:0.3rem 0.65rem
    - border-radius:999px; font-size:0.72rem; font-weight:700

    5 VARIANTS — each with rgba background, specific text color, rgba border:
    - .pill.ok: bg rgba(34,197,94,.15), color #86efac, border 1px solid rgba(34,197,94,.3)
    - .pill.todo: bg rgba(245,158,11,.15), color #fcd34d, border 1px solid rgba(245,158,11,.3)
    - .pill.doing: bg rgba(99,102,241,.15), color #a5b4fc, border 1px solid rgba(99,102,241,.3)
    - .pill.done: bg rgba(16,185,129,.18), color #6ee7b7, border 1px solid rgba(16,185,129,.3)
    - .pill.danger: bg rgba(239,68,68,.18), color #fda4af, border 1px solid rgba(239,68,68,.3)

    FILE 7: src/styles/inputs.css — Form inputs

    .input (RECON.md line 3660-3672):
    - width:100%; padding:0.95rem 1.1rem; border-radius:14px
    - background:#0b1330; border:1px solid var(--border); color:white
    - font-size:1rem; outline:none; transition:all 0.15s; font-family:Inter,sans-serif

    .input:focus (line 3678-3681):
    - border-color:#6366f1; box-shadow:0 0 0 4px rgba(99,102,241,.18)

    .input::placeholder (line 3686-3689): color:#6b7299

    textarea.input (line 3697-3699): min-height:90px; resize:vertical

    select.input (line 3704-3710):
    - appearance:none; custom dropdown arrow via inline SVG data URI (muted color #9aa3c7)
    - background-position:right 12px center; padding-right:36px

    FILE 8: src/styles/steps.css — Wizard step cards

    .step-card (RECON.md line 3597-3602):
    - position:relative; padding:1.5rem 1.5rem 1.5rem 5rem; border-radius:20px; transition:opacity 0.3s

    .step-card.disabled (line 3610-3613):
    - opacity:0.45; pointer-events:none

    .step-card.done .step-num (line 3619-3623):
    - background:linear-gradient(160deg, #34d399, #10b981)
    - box-shadow:0 6px 0 #065f46, inset 0 1px 0 rgba(255,255,255,.25)

    .step-num (line 3632-3647):
    - position:absolute; left:1.2rem; top:1.2rem; width:48px; height:48px; border-radius:14px
    - display:flex; align-items:center; justify-content:center; font-weight:800; font-size:1.25rem; color:white
    - background:linear-gradient(160deg, #7c83ff, #4f46e5)
    - box-shadow:0 6px 0 #312e81, inset 0 1px 0 rgba(255,255,255,.25)

    FILE 9: src/styles/layout.css — Header + Content area layout

    Sidebar transition (RECON.md line 3862): .sidebar { transition: transform 0.25s ease; }

    Header-specific styles (not already covered by Tailwind utilities):
    - The header's background + backdrop-filter are applied via inline style in index.html (Plan 01)
    - This file ensures .content-wrap behaves correctly

    FILE 10: src/styles/misc.css — Utility component classes

    .switch-tab (line 3725-3738): padding:0.55rem 1rem; border-radius:10px; font-weight:600; font-size:0.85rem; cursor:pointer; color:#9aa3c7
    .switch-tab.active: bg rgba(99,102,241,.2); color:white; box-shadow:inset 0 0 0 1px rgba(99,102,241,.4)

    .copy-row (line 3745-3766): display:flex; align-items:center; gap:0.5rem; padding:0.85rem 1rem; border-radius:14px; background:#0b1330; border:1px solid var(--border)
    .copy-row .key: font-size:0.7rem; color:#9aa3c7; text-transform:uppercase; letter-spacing:0.1em; min-width:120px
    .copy-row .val: flex:1; font-weight:700; word-break:break-word

    .empty (line 3771-3777): border:2px dashed rgba(255,255,255,.1); border-radius:18px; padding:2rem; text-align:center; color:var(--muted)

    .scrollbar (line 3782-3788): ::-webkit-scrollbar width 8px; ::-webkit-scrollbar-thumb bg #2a3460 border-radius 6px; ::-webkit-scrollbar-track bg transparent

    .file-drop (line 3792-3804): border:2px dashed rgba(99,102,241,.4); border-radius:20px; padding:2.5rem; text-align:center; cursor:pointer; transition:all 0.15s; background:rgba(99,102,241,.04)
    .file-drop:hover,dragover: bg rgba(99,102,241,.1); border-color:rgba(99,102,241,.7)

    .spinner (line 3898-3910): width:18px; height:18px; border-radius:50%; border:3px solid rgba(255,255,255,.2); border-top-color:white; animation:spin 0.8s linear infinite; display:inline-block; vertical-align:middle

    .pdf-canvas-wrap + .pdf-overlay-text (lines 3810-3855): PDF editor overlay styles — document verbatim from RECON.md. Used in Phase 03 Etapa 3.

    FILE 11: src/styles/animations.css — Keyframe animations

    @keyframes float (RECON.md line 3968-3971):
    - 0%,100% { transform:translateY(0); } 50% { transform:translateY(-6px); }
    .floaty { animation: float 4s ease-in-out infinite; }

    @keyframes spin (line 3983-3985):
    - to { transform:rotate(360deg); }
    (Applied by .spinner class in misc.css)

    @keyframes pulse-ring (line 4005-4009):
    - 0% { box-shadow:0 0 0 0 rgba(34,211,238,.6); }
    - 70% { box-shadow:0 0 0 14px rgba(34,211,238,0); }
    - 100% { box-shadow:0 0 0 0 rgba(34,211,238,0); }
    .pulse-ring { animation: pulse-ring 2s infinite; }
  </action>
  <verify>
    <automated>python3 -c "
import os

files = ['src/styles/navigation.css','src/styles/pills.css','src/styles/inputs.css',
         'src/styles/steps.css','src/styles/layout.css','src/styles/misc.css','src/styles/animations.css']
for f in files:
    assert os.path.exists(f), f'Missing: {f}'
    content = open(f).read()
    assert len(content) > 50, f'{f} too small'

# navigation.css
nav = open('src/styles/navigation.css').read()
assert '.nav-link' in nav
assert '.nav-link:hover' in nav
assert '.nav-link.active' in nav
assert '.nav-emoji' in nav

# pills.css
pills = open('src/styles/pills.css').read()
assert '.pill' in pills
assert 'border-radius: 999px' in pills or 'border-radius:999px' in pills
for v in ['ok','todo','doing','done','danger']:
    assert f'.pill.{v}' in pills or f'pill.{v}' in pills, f'Missing .pill.{v}'

# inputs.css
inp = open('src/styles/inputs.css').read()
assert '.input' in inp
assert '.input:focus' in inp or 'input:focus' in inp

# steps.css
steps = open('src/styles/steps.css').read()
assert '.step-card' in steps
assert '.step-num' in steps
assert '.step-card.disabled' in steps or 'step-card.disabled' in steps

# layout.css
layout = open('src/styles/layout.css').read()
assert '.sidebar' in layout or 'sidebar' in layout

# misc.css
misc = open('src/styles/misc.css').read()
assert '.copy-row' in misc
assert '.empty' in misc
assert '.spinner' in misc

# animations.css
anim = open('src/styles/animations.css').read()
assert '@keyframes float' in anim
assert '@keyframes spin' in anim
assert '@keyframes pulse-ring' in anim

print('ALL COMPONENT CSS CHECKS PASSED')
"</automated>
  </verify>
  <acceptance_criteria>
    - .nav-link: base style with 14px border-radius, hover with translateX(2px), active with indigo→cyan gradient
    - .nav-emoji: 36×36px icon container with subtle white gradient background
    - .pill: 999px border-radius, 5 color variants with rgba backgrounds and matching text/border colors
    - .input: dark background, focus ring with indigo border + 4px glow
    - select.input: custom SVG dropdown arrow, no browser-native appearance
    - .step-card: 5rem left padding for step-num, .disabled at 45% opacity, .done with green step-num
    - .step-num: absolute positioned 48×48px mini 3D badge with indigo gradient
    - .switch-tab, .copy-row, .empty, .scrollbar, .file-drop, .spinner: all matching RECON.md
    - @keyframes float (4s bobbing), spin (0.8s rotation), pulse-ring (2s expanding ring)
    - All styles use hardcoded hex values (not var() conversions) per Pitfall 5
  </acceptance_criteria>
  <done>All component CSS classes created: navigation, pills, inputs, steps, layout, misc utilities, and keyframe animations — matching RECON.md §6.2-6.4 specifications exactly.</done>
</task>

<task type="auto">
  <name>Task 02-05-03: Create Responsive CSS and CORS Proxy Layer</name>
  <files>
    src/styles/responsive.css
    src/proxy/index.js
  </files>
  <read_first>
    - RECON.md §6.5 Responsive Breakpoints (lines 4046-4113) — 1024px sidebar collapse with exact CSS
    - RECON.md §5.10 instalarProxy() (lines 3001-3023) — fetch monkey-patch with URL rewriting rules
    - RECON.md §2.5 CORS Proxy Layer (lines 1465-1522) — URL rewriting patterns, Netlify integration
    - RESEARCH.md § Pattern 5: CORS Proxy Layer (lines 460-486) — Code Example
    - D-02 — HTML + CDN, deploy to Netlify
  </read_first>
  <action>
    Per D-04, create the responsive breakpoint CSS and CORS proxy module.

    FILE 12: src/styles/responsive.css — Mobile Sidebar Collapse

    Copy EXACTLY from RECON.md §6.5 (lines 4053-4063):

    ```css
    @media (max-width: 1024px) {
      .sidebar {
        position: fixed;
        inset: 0 auto 0 0;
        z-index: 50;
        transform: translateX(-100%);
        width: 280px;
      }
      .sidebar.open { transform: translateX(0); }
      .content-wrap { margin-left: 0 !important; }
    }
    ```

    Backdrop styles (RECON.md lines 3879-3888):
    ```css
    .backdrop { display: none; }
    .backdrop.open {
      display: block;
      position: fixed;
      inset: 0;
      background: rgba(0,0,0,.55);
      z-index: 40;
    }
    ```

    The combined effect:
    - Desktop (≥1025px): sidebar visible, static in flex layout; backdrop hidden
    - Mobile (≤1024px): sidebar hidden off-screen (translateX(-100%)); toggleSidebar(true) adds .open → slides in; backdrop appears at z-index:40 behind sidebar (z-index:50)
    - Transition: .sidebar { transition: transform 0.25s ease; } (defined in navigation.css or layout.css)

    FILE 13: src/proxy/index.js — CORS Proxy Layer

    instalarProxy(): Monkey-patches window.fetch to rewrite API URLs per RECON.md §5.10.

    Implementation (matches RECON.md lines 3001-3023 exactly):
    - If location.protocol === 'file:' → return early (local dev, no proxy needed)
    - Save original fetch: const orig = window.fetch
    - Override window.fetch with wrapper function(url, opts)
    - If url is a string:
      - Starts with 'https://api.cloudflare.com' → replace with '/cf-api'
      - Else starts with 'https://api.sms24h.org' → replace with '/sms-api'
      - Else starts with 'https://sms24h.org' → replace with '/sms-api'
    - Call original fetch with (possibly rewritten) url and opts
    - Return the fetch Promise (pass through result)

    URL rewriting rules (matching §2.5 exactly):
    https://api.cloudflare.com/client/v4/... → /cf-api/client/v4/...
    https://api.sms24h.org/stubs/handler_api?... → /sms-api/stubs/handler_api?...
    https://sms24h.org/... → /sms-api/...

    Edge cases:
    - file: protocol → skip (local dev — API calls will hit CORS, expected behavior)
    - Request object as URL → proxy does nothing (only handles string URLs, matching original)
    - Headers not modified
    - No error handling — if original fetch throws, error propagates unchanged

    EXPORT: export { instalarProxy }
  </action>
  <verify>
    <automated>python3 -c "
import os

# Verify responsive.css
resp = open('src/styles/responsive.css').read()
assert '@media (max-width: 1024px)' in resp, 'Missing 1024px breakpoint'
assert 'translateX(-100%)' in resp, 'Missing sidebar off-screen transform'
assert '.sidebar.open' in resp, 'Missing .sidebar.open'
assert '.content-wrap' in resp, 'Missing .content-wrap'
assert '.backdrop' in resp, 'Missing .backdrop'
assert 'rgba(0,0,0,.55)' in resp, 'Missing backdrop color'
print('responsive.css checks passed')
" && echo "---"

# Verify proxy module
node --input-type=module -e "
import { instalarProxy } from './src/proxy/index.js';
console.assert(typeof instalarProxy === 'function', 'instalarProxy is a function');

// Test URL rewriting logic (unit test without DOM)
// Save original fetch
const origFetch = globalThis.fetch;
let capturedUrl = null;
globalThis.fetch = function(url, opts) { capturedUrl = url; return Promise.resolve({ok:true}); };

// Mock location
const origLocation = globalThis.location;
globalThis.location = { protocol: 'https:' };

instalarProxy();

// Test Cloudflare URL rewriting
globalThis.fetch('https://api.cloudflare.com/client/v4/accounts/test', {});
console.assert(capturedUrl === '/cf-api/client/v4/accounts/test', 'Cloudflare URL rewritten: ' + capturedUrl);

// Test SMS URL rewriting
globalThis.fetch('https://api.sms24h.org/stubs/handler_api?key=test', {});
console.assert(capturedUrl === '/sms-api/stubs/handler_api?key=test', 'SMS24h URL rewritten');

// Test non-proxied URL passes through
globalThis.fetch('https://brasilapi.com.br/api/cnpj/v1/123', {});
console.assert(capturedUrl === 'https://brasilapi.com.br/api/cnpj/v1/123', 'Non-proxy URL unchanged');

// Restore
globalThis.fetch = origFetch;
if (origLocation) globalThis.location = origLocation;

console.log('ALL PROXY TESTS PASSED');
" && echo "Proxy module OK"</automated>
  </verify>
  <acceptance_criteria>
    - @media (max-width:1024px): sidebar becomes fixed overlay, slides in/out with .open class
    - .backdrop: hidden by default, .open shows full-screen dark overlay at z-index:40
    - .sidebar.open: translateX(0) slides sidebar into view
    - .content-wrap: margin-left:0 at mobile (content fills full width)
    - instalarProxy: skips on file: protocol
    - instalarProxy: rewrites api.cloudflare.com → /cf-api
    - instalarProxy: rewrites api.sms24h.org → /sms-api
    - instalarProxy: rewrites sms24h.org → /sms-api
    - instalarProxy: non-matching URLs pass through unchanged
    - instalarProxy: preserves original fetch behavior (returns Promise, passes opts through)
  </acceptance_criteria>
  <done>Responsive breakpoint at 1024px collapses sidebar into a sliding overlay with backdrop. CORS proxy monkey-patches fetch to rewrite Cloudflare/SMS24h URLs for Netlify deployment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS → Browser rendering | All CSS is static, from developer-controlled files — no injection surface |
| fetch monkey-patch → All API calls | instalarProxy() intercepts every fetch call — URL rewriting could theoretically be abused |
| CDN → CSS | No external CSS dependencies (Tailwind is CDN, but from trusted source) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-18 | Tampering | instalarProxy() URL rewriting | accept | Only rewrites 3 known URL prefixes (Cloudflare API, SMS24h API, SMS24h non-API). Does not intercept other domains. String-prefix matching — no regex injection risk. |
| T-02-19 | Elevation of Privilege | window.fetch override | accept | Monkey-patching fetch is by design — matches original pattern. Original fetch preserved in closure and called with (possibly) rewritten URL. No credentials or headers modified. |
| T-02-20 | Denial of Service | CSS @keyframes infinite animations | accept | 3 infinite animations (float, spin, pulse-ring). Modern browsers efficiently pause off-screen animations. Accept for visual fidelity. |
| T-02-21 | Information Disclosure | CSS hardcoded color values | accept | Colors are design tokens — no sensitive data. Accept per original pattern. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs — vanilla JS + CSS only per D-02. |
</threat_model>

<verification>
**Visual verification (browser):**
1. Open http://localhost:3000 — verify dark navy background, white-blue text
2. Check DevTools → Elements → Styles: :root shows 13 custom properties
3. Hover over sidebar nav-links: verify translateX(2px) + background highlight
4. Click a nav-link: verify .active gradient appears on the clicked link
5. Resize browser to ≤1024px: verify sidebar disappears, hamburger button visible (in header)
6. Click hamburger: verify sidebar slides in from left with backdrop overlay
7. Click backdrop: verify sidebar closes
8. Check DevTools → Console: `instalarProxy` should have been called (no errors)
9. Check DevTools → Network: verify fetch is monkey-patched (call `fetch('https://api.cloudflare.com/test')` and see URL rewritten to `/cf-api/test`)

**CSS completeness check:**
```bash
# Count CSS files
ls src/styles/*.css | wc -l  # should be 12

# Verify no CSS file is empty
for f in src/styles/*.css; do
  lines=$(wc -l < "$f")
  if [ "$lines" -lt 3 ]; then echo "WARNING: $f has only $lines lines"; fi
done
echo "All 12 CSS files are non-trivial"
```
</verification>

<success_criteria>
- [ ] All 12 CSS files created with matching RECON.md §6 content
- [ ] 13 :root custom properties with exact hex values
- [ ] 23 component class families (glass, grad-card, neon, ring-glow, btn-3d, icon-cube, pill, nav-link, step-card, input, switch-tab, copy-row, empty, scrollbar, file-drop, pdf-canvas-wrap, pdf-overlay-text, sidebar, backdrop, floaty, spinner, pulse-ring, grad-text)
- [ ] 19 color variants across 3 component families (8 btn-3d + 6 icon-cube + 5 pill)
- [ ] 4 @keyframes animations (float, spin, pulse-ring, pulse)
- [ ] 6 CSS transitions on interactive components
- [ ] @media (max-width:1024px) sidebar collapse with slide animation + backdrop overlay
- [ ] Hardcoded hex values preserved (not converted to var()) per Pitfall 5
- [ ] instalarProxy() rewrites 3 URL patterns, skips on file: protocol
- [ ] Application renders with correct dark theme, glassmorphism, 3D buttons, and icon cubes
</success_criteria>

<output>
Create `.planning/phases/02-foundation/02-05-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

| Artifact | Path | Phase Requirement |
|----------|------|-------------------|
| CSS Custom Properties | src/styles/theme.css | UI-02 |
| Glassmorphism + Glow | src/styles/components.css | UI-02 |
| 3D Button System | src/styles/buttons.css | UI-03 |
| Icon Cube System | src/styles/icon-cube.css | UI-04 |
| Navigation Styles | src/styles/navigation.css | CORE-02 |
| Pill/Badge Styles | src/styles/pills.css | UI-02 |
| Input Styles | src/styles/inputs.css | UI-02 |
| Step Card Styles | src/styles/steps.css | UI-02 |
| Layout Styles | src/styles/layout.css | CORE-02, CORE-04 |
| Misc Utilities CSS | src/styles/misc.css | UI-02 |
| Animation Keyframes | src/styles/animations.css | UI-02 |
| Responsive Breakpoints | src/styles/responsive.css | UI-01 |
| CORS Proxy Layer | src/proxy/index.js | CORE-09 |
