---
phase: 02-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - src/main.js
  - _redirects
  - .gitignore
autonomous: true
requirements: [CORE-02, CORE-04]
user_setup:
  - service: local-dev-server
    why: "ES modules require HTTP server (file:// protocol blocked by CORS)"
    env_vars: []
    dashboard_config:
      - task: "Run npx serve . -p 3000 or python3 -m http.server 3000"
        location: "Terminal in project root"

must_haves:
  truths:
    - "index.html loads without console errors when served via HTTP"
    - "Tailwind CSS v3 compiles utility classes from the CDN"
    - "Google Fonts Inter and Sora load correctly"
    - "Static DOM shell renders sidebar, header, toast, modal, backdrop, and view container"
    - "main.js executes bootstrap sequence: instalarProxy → refreshHeaderStatus → go('dashboard')"
  artifacts:
    - path: "index.html"
      provides: "Static HTML shell with all persistent DOM elements and CDN script tags"
      contains: "<aside id=\"sidebar\">"
      contains: "<header"
      contains: "cdn.tailwindcss.com/3.4.0"
      contains: "<script type=\"module\" src=\"src/main.js\">"
    - path: "src/main.js"
      provides: "Application entry point — imports all modules, exposes globals, boots app"
      exports: "bootstrap sequence calling instalarProxy(), refreshHeaderStatus(), go('dashboard')"
    - path: "_redirects"
      provides: "Netlify CORS proxy rewrite rules"
      contains: "/cf-api/*"
      contains: "/sms-api/*"
    - path: ".gitignore"
      provides: "Git ignore rules for the project"
      contains: ".planning/"
  key_links:
    - from: "index.html <script type=\"module\" src=\"src/main.js\">"
      to: "src/main.js"
      via: "ES module import — main.js is the single entry point"
    - from: "src/main.js window.go = go"
      to: "src/router/index.js"
      via: "ES module import → window exposure for inline onclick handlers"
    - from: "index.html <link> tags"
      to: "src/styles/*.css"
      via: "CSS files loaded via <link> — no build step"
---

<objective>
Create the complete project scaffold: directory structure per D-01 (src/stores/, src/router/, src/views/, src/widgets/, src/utils/, src/styles/), the static index.html shell with ALL persistent DOM elements (backdrop, sidebar, header, toast, modal, view container), the main.js entry point with full module imports and window exposure, Tailwind CSS v3 CDN config, Netlify _redirects for CORS proxy, and .gitignore.

Purpose: This plan establishes the physical project skeleton — every subsequent plan creates module files that main.js already imports, and CSS files that index.html already links. The scaffold is the contract: later plans fill in the modules without touching index.html or main.js.

Output: Fully functional project shell that renders the sidebar, header, and empty view container. Clicking sidebar links won't navigate yet (router module doesn't exist until Plan 03), but the DOM structure is complete and pixel-identical to the original per RECON.md §1.1.
</objective>

<execution_context>
@/home/matspectrum-ai/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/matspectrum-ai/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-foundation/02-CONTEXT.md
@.planning/phases/02-foundation/02-RESEARCH.md
@.planning/phases/01-reconnaissance-extraction/RECON.md (authoritative spec — §1.1 Static Shell, §1.1.2 Sidebar, §1.1.3 Header, §1.1.4 Toast, §1.1.5 Modal, §6.1 CSS Custom Properties, §6.6 Tailwind Config, §5.10 Boot/Proxy)
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 02-01-01: Create Directory Structure and index.html Shell</name>
  <files>
    index.html
    .gitignore
  </files>
  <read_first>
    - RECON.md §1.1 Static Shell (lines 60-200) — exact DOM structure for backdrop, sidebar with 8 nav-links + 3 category headers + tip card, header with title/subtitle/pills/avatar, toast container, modal container, view container
    - RECON.md §1.1.2 Sidebar — complete nav-link element structure with data-route, onclick, nav-emoji, categories
    - RECON.md §1.1.3 Header — sticky header with #page-title, #page-subtitle, #cf-status pill, #sms-status pill, hamburger button, avatar
    - RECON.md §6.6 Tailwind CDN — config script BEFORE CDN script, font family extensions, CDN URL pinned to 3.4.0
    - RESEARCH.md § Standard Stack — Google Fonts CDN URL, Inter weights, Sora weights
    - RESEARCH.md Pitfall 2 — ES modules require HTTP server, file:// protocol causes CORS
    - RESEARCH.md Pitfall 3 — Tailwind CDN version pinning to 3.4.0
    - D-01 (CONTEXT.md) — directory structure: src/stores/, src/router/, src/views/, src/widgets/, src/utils/, src/styles/
    - D-02 (CONTEXT.md) — HTML + CDN only, no Vite/npm/bundler
  </read_first>
  <action>
    Per D-01, create directory structure: src/stores/, src/router/, src/views/, src/widgets/, src/utils/, src/styles/, src/proxy/.

    Create .gitignore with: .planning/ (GSD planning directory), node_modules/, .DS_Store, *.log.

    Create index.html — the complete static DOM shell, matching RECON.md §1.1 EXACTLY. Structure:

    1. &lt;head&gt;: charset UTF-8, viewport meta, title "Laboratório de BMS", Google Fonts link for Inter (400;500;600;700;800) and Sora (600;700;800) with display=swap, Tailwind config script (tailwind.config = { theme: { extend: { fontFamily: { display: ['Sora','sans-serif'], sans: ['Inter','sans-serif'] } } } }), Tailwind CDN script src="https://cdn.tailwindcss.com/3.4.0" (per Pitfall 3 — pinned to prevent version drift).

    Line order for Tailwind: config script first, then CDN script — per RESEARCH.md Code Examples § Tailwind CDN Config.

    2. &lt;body class="min-h-screen"&gt;:
       a. Backdrop: &lt;div id="backdrop" class="backdrop" onclick="toggleSidebar(false)"&gt;&lt;/div&gt;
       b. Sidebar: &lt;aside id="sidebar" class="sidebar w-[280px] shrink-0 p-4 border-r border-white/5" style="background:linear-gradient(180deg,#0c1330,#0a0f24);"&gt; containing:
          - Logo: div with .icon-cube.purple (🧪, 52×52px) + "LABORATÓRIO&lt;br&gt;&lt;span class='grad-text'&gt;DE BMS 🎈&lt;/span&gt;" + "ADM: João Victor" subtitle
          - &lt;nav class="space-y-1.5"&gt; with 8 nav-links EXACTLY as documented: dashboard (🏠 Início, .active by default), then category header "FLUXO PRINCIPAL" with etapa1/etapa2/etapa3, category header "DADOS" with banco/planilha, category header "SISTEMA" with config/ajuda
          - Each nav-link: &lt;div class="nav-link" data-route="X" onclick="go('X')"&gt; with &lt;span class="nav-emoji"&gt;icon&lt;/span&gt; + label text
          - Tip card: &lt;div class="glass mt-6 p-4 rounded-2xl"&gt; with .icon-cube.cyan ⭐ + tip text about capital social R$ 10k-50k
       c. Content wrapper: &lt;main class="content-wrap flex-1 min-w-0"&gt;
          - Header: &lt;header class="sticky top-0 z-30 px-4 sm:px-8 py-4 flex items-center justify-between border-b border-white/5" style="background:rgba(11,16,32,.85); backdrop-filter:blur(12px);"&gt; with left section (hamburger button .lg:hidden.btn-3d.ghost.sm with onclick="toggleSidebar(true)", #page-title "🏠 Início", #page-subtitle "Bem-vindo, João Victor!"), right section (#cf-status.pill.danger "⚠️ Cloudflare", #sms-status.pill.danger "⚠️ SMS24h", avatar .icon-cube.purple "JV")
          - &lt;section id="view" class="p-4 sm:p-8 max-w-7xl mx-auto"&gt;&lt;/section&gt;
       d. Toast: &lt;div id="toast" class="hidden fixed bottom-6 left-1/2 -translate-x-1/2 z-[100] glass px-5 py-3 rounded-2xl flex items-center gap-3 shadow-2xl"&gt; with #toast-icon and #toast-msg
       e. Modal: &lt;div id="modal-back" class="hidden" onclick="if(event.target===this)closeModal()"&gt; containing backdrop + #modal-body.glass.rounded-3xl

    3. &lt;link&gt; tags for ALL CSS files from Plan 05 (they won't exist yet — Plan 05 creates them): theme.css, components.css, buttons.css, icon-cube.css, navigation.css, pills.css, inputs.css, steps.css, layout.css, misc.css, animations.css, responsive.css. Order: theme.css first (custom properties), then component CSS files, then layout.css, responsive.css last.

    4. &lt;script type="module" src="src/main.js"&gt; as the last element before &lt;/body&gt;.

    Style &lt;link&gt; tags placed in &lt;head&gt; before the Tailwind script. Tailwind config script + CDN script ALSO in &lt;head&gt; (not body) — matching the original.

    CRITICAL: All element IDs must match RECON.md exactly: backdrop, sidebar, page-title, page-subtitle, cf-status, sms-status, view, toast, toast-icon, toast-msg, modal-back, modal-body. These are referenced by JS modules in Plans 02-04.

    inline onclick handlers use global function names: go('route'), toggleSidebar(true/false), closeModal(). These will be exposed to window by main.js.
  </action>
  <verify>
    <automated>python3 -c "
import os
# Verify directory structure
for d in ['src/stores','src/router','src/views','src/widgets','src/utils','src/styles','src/proxy']:
    assert os.path.isdir(d), f'Missing directory: {d}'
# Verify index.html exists and contains key elements
html = open('index.html').read()
assert '<aside id=\"sidebar\"' in html, 'Missing sidebar'
assert 'id=\"page-title\"' in html, 'Missing page-title'
assert 'id=\"backdrop\"' in html, 'Missing backdrop'
assert 'id=\"toast\"' in html, 'Missing toast'
assert 'id=\"modal-back\"' in html, 'Missing modal'
assert 'id=\"view\"' in html, 'Missing view'
assert 'cdn.tailwindcss.com/3.4.0' in html, 'Missing pinned Tailwind CDN'
assert 'script type=\"module\" src=\"src/main.js\"' in html, 'Missing module entry'
assert 'data-route=\"dashboard\"' in html, 'Missing dashboard nav-link'
assert 'data-route=\"etapa1\"' in html, 'Missing etapa1 nav-link'
assert 'data-route=\"config\"' in html, 'Missing config nav-link'
assert 'data-route=\"ajuda\"' in html, 'Missing ajuda nav-link'
print('ALL CHECKS PASSED')
"</automated>
  </verify>
  <acceptance_criteria>
    - All 7 src/ subdirectories exist
    - index.html contains all 6 persistent DOM elements (backdrop, sidebar, header, toast, modal, view)
    - All 8 nav-links present with correct data-route attributes and onclick handlers
    - 3 category headers (FLUXO PRINCIPAL, DADOS, SISTEMA) present
    - Tailwind CDN pinned to 3.4.0 with JS config for Inter/Sora font families
    - Google Fonts link loads Inter (400-800) and Sora (600-800)
    - All 12 CSS &lt;link&gt; tags present in correct order (theme.css first, responsive.css last)
    - Static shell matches RECON.md §1.1 element-for-element
  </acceptance_criteria>
  <done>Directory structure created, index.html renders static shell with all DOM elements when opened via HTTP server. Sidebar, header, toast, modal, and view container are visible in the browser.</done>
</task>

<task type="auto">
  <name>Task 02-01-02: Create main.js Entry Point with Full Module Imports and Bootstrap</name>
  <files>
    src/main.js
  </files>
  <read_first>
    - RESEARCH.md § Code Examples — Complete main.js Entry Point (lines 595-645)
    - RESEARCH.md § Pattern 2 — Global Exposure for Inline Handlers (lines 294-327)
    - RESEARCH.md Pitfall 1 — ES Module Global Scope Breakage (lines 515-531)
    - RECON.md §5.10 Boot/Proxy Functions — instalarProxy() and bootstrap sequence
    - RECON.md §4.3 go() — initial route navigation to 'dashboard'
    - D-04 (CONTEXT.md) — same function names and signatures as original
  </read_first>
  <action>
    Per D-04 and RESEARCH.md Pattern 2, create src/main.js as the single application entry point.

    This file performs ALL module imports (from modules that don't exist yet — Plans 02-05 will create them), exposes 8 global functions to window for inline onclick handlers, and executes the bootstrap sequence.

    Structure per RESEARCH.md § Complete main.js Entry Point:

    1. IMPORT ALL MODULES (these files don't exist yet — Plans 02-05 create them):
       - from './stores/data.js': getDB, saveDB, getSettings, saveSettings
       - from './router/index.js': go, toggleSidebar, VIEWS
       - from './widgets/toast.js': toast
       - from './widgets/modal.js': openModal, closeModal
       - from './utils/clipboard.js': copyText
       - from './utils/string.js': escapeHTML, onlyDigits
       - from './utils/header.js': refreshHeaderStatus
       - from './proxy/index.js': instalarProxy
       - View stubs: import init* functions from each './views/*.js' (initDashboard, initEtapa1, initEtapa2, initEtapa3, initBanco, initPlanilha, initConfig, initAjuda)

    2. POPULATE VIEWS REGISTRY: Call each init*() function to register VIEWS[route] functions. Each init function assigns its view renderer to the imported VIEWS object. This matches the original's progressive assignment pattern.

    3. EXPOSE GLOBAL FUNCTIONS (CRITICAL — per Pitfall 1): Attach to window the 8 functions needed by inline onclick handlers in index.html and widget-generated HTML:
       window.go = go;
       window.toggleSidebar = toggleSidebar;
       window.closeModal = closeModal;
       window.copyText = copyText;
       window.escapeHTML = escapeHTML;
       window.onlyDigits = onlyDigits;
       window.toast = toast;
       window.openModal = openModal;

    4. BOOTSTRAP SEQUENCE (matches original lines 2089-2132, per RECON.md §5.10):
       a. instalarProxy() — monkey-patch fetch for CORS proxy
       b. refreshHeaderStatus() — update API status pills
       c. go('dashboard') — initial route render

    IMPORTANT: autoConectarTokens() is intentionally OMITTED — the original hardcodes credentials (per RESEARCH.md § State of the Art). The clone uses empty settings defaults (getSettings() returns {} when key missing). Users configure their own tokens via the Config view.

    Export nothing — this file is the top-level entry point, not a library module.

    If a module import fails because the file doesn't exist yet, the browser will throw at load time. This is expected until Plans 02-05 complete. The plan executor should run Plans 02-05 before verifying the app in the browser.
  </action>
  <verify>
    <automated>python3 -c "
main = open('src/main.js').read()
# Verify all required imports
assert \"from './stores/data.js'\" in main, 'Missing stores import'
assert \"from './router/index.js'\" in main, 'Missing router import'
assert \"from './widgets/toast.js'\" in main, 'Missing toast import'
assert \"from './widgets/modal.js'\" in main, 'Missing modal import'
assert \"from './utils/clipboard.js'\" in main, 'Missing clipboard import'
assert \"from './utils/string.js'\" in main, 'Missing string utils import'
assert \"from './utils/header.js'\" in main, 'Missing header import'
assert \"from './proxy/index.js'\" in main, 'Missing proxy import'
# Verify all 8 view imports present
for v in ['dashboard','etapa1','etapa2','etapa3','banco','planilha','config','ajuda']:
    assert f\"from './views/{v}.js'\" in main, f'Missing view import: {v}'
# Verify window exposure (all 8 globals)
for fn in ['go','toggleSidebar','closeModal','copyText','escapeHTML','onlyDigits','toast','openModal']:
    assert f'window.{fn} = {fn}' in main or f'window.{fn} =' in main, f'Missing window.{fn} exposure'
# Verify bootstrap sequence
assert 'instalarProxy()' in main, 'Missing instalarProxy() call'
assert 'refreshHeaderStatus()' in main, 'Missing refreshHeaderStatus() call'
assert \"go('dashboard')\" in main, 'Missing go(dashboard) call'
# Verify autoConectarTokens is NOT present
assert 'autoConectarTokens' not in main, 'autoConectarTokens should be OMITTED — hardcoded credentials'
print('ALL CHECKS PASSED')
"</automated>
  </verify>
  <acceptance_criteria>
    - main.js imports from all 5 layer directories (stores, router, views, widgets, utils, proxy)
    - All 8 view init functions imported and called to populate VIEWS registry
    - All 8 global functions exposed to window (go, toggleSidebar, closeModal, copyText, escapeHTML, onlyDigits, toast, openModal)
    - Bootstrap sequence executes instalarProxy → refreshHeaderStatus → go('dashboard') in that order
    - autoConectarTokens is NOT imported or called
    - File is syntactically valid JavaScript (no syntax errors)
  </acceptance_criteria>
  <done>main.js imports all application modules, exposes globals to window, and performs bootstrap. The file is syntactically correct and will execute properly once all imported modules exist.</done>
</task>

<task type="auto">
  <name>Task 02-01-03: Create _redirects for Netlify CORS Proxy</name>
  <files>
    _redirects
  </files>
  <read_first>
    - RECON.md §2.5 CORS Proxy Layer (lines 1465-1522) — URL rewriting rules, Netlify _redirects format
    - RECON.md §5.10 instalarProxy() — URL rewriting patterns (api.cloudflare.com → /cf-api, api.sms24h.org → /sms-api, sms24h.org → /sms-api)
    - RESEARCH.md § Open Questions #2 — _redirects file for Netlify deployment
    - D-02 — HTML + CDN only, deploy to Netlify
  </read_first>
  <action>
    Per RECON.md §2.5, create _redirects file in the project root for Netlify deployment. This file configures Netlify's reverse proxy to forward /cf-api/* and /sms-api/* requests to their upstream APIs, bypassing CORS restrictions.

    Content exactly matches the original's proxy rules:

    /cf-api/*  https://api.cloudflare.com/:splat  200
    /sms-api/* https://api.sms24h.org/:splat  200
    /sms-api/* https://sms24h.org/:splat  200

    The /sms-api/ rule appears twice because instalarProxy() rewrites TWO different upstream domains (api.sms24h.org and sms24h.org) to the same proxy path. Netlify's _redirects uses first-match, so this may need to be handled differently — but RECON.md documents it this way, so replicate exactly.

    For LOCAL DEVELOPMENT: instalarProxy() (Plan 05) checks for file: protocol and skips proxying. When served via npx serve, fetch calls to /cf-api/* and /sms-api/* will hit 404 (no Netlify proxy). This is expected — API calls work only in production or with a local proxy server. Document this in a comment at the top of the file.

    Comment header:
    # Netlify CORS Proxy — rewrites /cf-api/* and /sms-api/* to upstream APIs
    # Local dev: instalarProxy() skips on file:// protocol. Use Netlify CLI (netlify dev) for local proxy.
  </action>
  <verify>
    <automated>python3 -c "
content = open('_redirects').read()
assert '/cf-api/*' in content, 'Missing Cloudflare proxy rule'
assert '/sms-api/*' in content, 'Missing SMS proxy rule'
assert 'api.cloudflare.com' in content, 'Missing Cloudflare upstream URL'
assert 'api.sms24h.org' in content, 'Missing SMS24h upstream URL'
lines = [l.strip() for l in content.split('\n') if l.strip() and not l.strip().startswith('#')]
# Verify at least 3 rewrite rules (2 sms + 1 cf)
cf_lines = [l for l in lines if 'cf-api' in l]
sms_lines = [l for l in lines if 'sms-api' in l]
assert len(cf_lines) >= 1, 'Need at least 1 Cloudflare proxy rule'
assert len(sms_lines) >= 2, 'Need at least 2 SMS proxy rules (api.sms24h.org + sms24h.org)'
print('ALL CHECKS PASSED')
"</automated>
  </verify>
  <acceptance_criteria>
    - _redirects file exists in project root
    - Contains Cloudflare Pages proxy rule: /cf-api/* → https://api.cloudflare.com/:splat 200
    - Contains SMS24h proxy rule: /sms-api/* → https://api.sms24h.org/:splat 200
    - Contains secondary SMS24h rule for sms24h.org domain
    - Comment header explains local dev limitations
  </acceptance_criteria>
  <done>_redirects file created with CORS proxy rewrite rules matching the original's instalarProxy() URL rewriting patterns. File is at project root for Netlify auto-detection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN → Browser | Tailwind CSS v3.4.0 loaded from cdn.tailwindcss.com — trusted Tailwind Labs distribution |
| CDN → Browser | Google Fonts loaded from fonts.googleapis.com — trusted Google CDN |
| File system → Browser | index.html and src/*.js served from project directory — developer-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Tailwind CDN script (cdn.tailwindcss.com/3.4.0) | accept | Official Tailwind Labs CDN with pinned version. Risk of CDN compromise is low and outside our control. |
| T-02-02 | Tampering | Google Fonts CDN (fonts.googleapis.com) | accept | Official Google CDN. Font loading failure gracefully falls back to browser sans-serif. |
| T-02-03 | Information Disclosure | inline onclick handlers in index.html | accept | Handlers reference global functions only — no sensitive data in HTML attributes. Values passed are route names and booleans. |
| T-02-04 | Denial of Service | _redirects Netlify proxy config | accept | Proxy rules are static — no dynamic URL construction. Netlify handles rate limiting at infrastructure level. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — CDN only per D-02. No package legitimacy audit needed. |
</threat_model>

<verification>
**Phase-level check (after all 5 plans complete):** Open http://localhost:3000 in browser. Verify:
1. Sidebar renders with 8 nav-links, 3 category headers, tip card
2. Header renders with title "🏠 Início", subtitle, Cloudflare/SMS24h danger pills, JV avatar
3. Toast container exists (hidden by default)
4. Modal container exists (hidden by default)
5. View section is empty (no router module yet — will show "Dashboard placeholder" after Plan 03)
6. No console errors (except expected "Failed to load module" if dependent modules not yet created)
7. Tailwind utility classes compile (check: elements have Tailwind-generated styles)
8. Google Fonts Inter and Sora are applied
</verification>

<success_criteria>
- [ ] All 7 src/ subdirectories exist
- [ ] index.html contains complete static DOM shell matching RECON.md §1.1
- [ ] 8 nav-links with correct data-route attributes and onclick handlers
- [ ] 3 category headers (FLUXO PRINCIPAL, DADOS, SISTEMA)
- [ ] Sticky header with blur effect, dynamic title/subtitle elements, API status pills
- [ ] Toast and modal containers with correct IDs and hidden state
- [ ] Tailwind CSS v3 CDN pinned to 3.4.0 with Inter/Sora font config
- [ ] Google Fonts link loads Inter (400-800) and Sora (600-800)
- [ ] main.js imports all modules, exposes 8 globals to window, executes bootstrap sequence
- [ ] _redirects file with CORS proxy rules for Cloudflare and SMS24h
- [ ] .gitignore excludes .planning/ directory
- [ ] Project serves without errors via npx serve . -p 3000 (once all 5 plans complete)
</success_criteria>

<output>
Create `.planning/phases/02-foundation/02-01-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

| Artifact | Path | Phase Requirement |
|----------|------|-------------------|
| HTML Shell | index.html | CORE-02 (sidebar), CORE-04 (header) |
| App Entry Point | src/main.js | CORE-01 (bootstrap), CORE-09 (proxy import) |
| Netlify Proxy Config | _redirects | CORE-09 |
| Git Ignore | .gitignore | Infrastructure |
| Directory Structure | src/{stores,router,views,widgets,utils,styles,proxy}/ | D-01 |
</target>
