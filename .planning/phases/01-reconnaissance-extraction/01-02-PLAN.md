---
phase: 01-reconnaissance-extraction
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - .planning/phases/01-reconnaissance-extraction/RECON.md
autonomous: true
requirements: [RECON-01, RECON-04]
user_setup: []

must_haves:
  truths:
    - "The complete DOM tree of the static shell (sidebar, header, modals, toast container) is documented with element hierarchy, data-attributes, CSS classes, and event handlers"
    - "Every one of the 8 view content areas is documented in ALL states (loading, empty, populated, error) with conditional element visibility rules"
    - "All 8 routes are mapped with exact VIEWS registry entries, go() function implementation, title/subtitle update logic, and active nav-link CSS toggling"
    - "Navigation flow is documented: what go() does step-by-step, how history is managed, and what side effects occur on route change"
    - "Every DOM element with interactive behavior has its event handlers documented via getEventListeners() pattern analysis"
  artifacts:
    - path: ".planning/phases/01-reconnaissance-extraction/RECON.md"
      provides: "Section 2: DOM Tree & Component Hierarchy AND Section 3: Rotas & Sistema de Navegação"
      contains: "Static shell elements, 8 view DOM trees with all state variants, ROUTES array, VIEWS registry, go() function specification, navigation behavior"
  key_links:
    - from: "RECON.md §2.x View DOM tree"
      to: "RECON.md §1 (Plan 01) localStorage/state"
      via: "Each view element annotated with which state keys control conditional visibility"
    - from: "RECON.md §3 ROUTES array"
      to: "RECON.md §2.1 Shell sidebar nav-links"
      via: "data-route attributes match ROUTES.path values"
    - from: "RECON.md §3 VIEWS registry"
      to: "RECON.md §5 (future Plan 03) Business Logic Functions"
      via: "Each VIEWS entry points to the view function documented in §5"

security_enforcement: false
---

<objective>
Extract and document the complete DOM structure of the original system: static shell elements that persist across routes, the DOM tree of all 8 views in every state variant, and the full routing/navigation system (ROUTES array, VIEWS registry, go() function, navigation behavior).

Purpose: The DOM tree reveals the component hierarchy, interactive elements, and conditional rendering logic. The route system reveals the app's orchestration layer — how views are mounted, how navigation state is managed, and how the URL/history is handled. Together they provide the structural and orchestration specification for Phase 2 (Foundation).

Output: RECON.md §2 (DOM Tree & Component Hierarchy, ~300+ lines) and §3 (Rotas & Sistema de Navegação, ~150+ lines).
</objective>

<execution_context>
@/home/matspectrum-ai/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/matspectrum-ai/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-reconnaissance-extraction/RECON.md
@.planning/phases/01-reconnaissance-extraction/data/raw-source.html
@.planning/phases/01-reconnaissance-extraction/01-RESEARCH.md
@.planning/research/FEATURES.md
@.planning/phases/01-reconnaissance-extraction/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 01-02-01: Static Shell DOM Extraction — Sidebar, Header, Modals, Toast</name>
  <files>.planning/phases/01-reconnaissance-extraction/RECON.md</files>
  <read_first>
    - .planning/phases/01-reconnaissance-extraction/RECON.md §1 (localStorage/state from Plan 01 — data model context)
    - .planning/phases/01-reconnaissance-extraction/data/raw-source.html (full source)
    - .planning/phases/01-reconnaissance-extraction/01-RESEARCH.md § Architecture Patterns — Pattern 1: DOM Tree Extraction Workflow
    - .planning/phases/01-reconnaissance-extraction/01-RESEARCH.md § Common Pitfalls — Pitfall 1: DOM Extraction Without Context
    - .planning/research/FEATURES.md § Table Stakes (rows for Sidebar Navigation, Sticky Header, Toast, Modal)
  </read_first>
  <action>
    Per D-02 (complete depth) and D-03 (manual DevTools extraction via source analysis):

    **IMPORTANT:** The executor cannot open Chrome DevTools. ALL extraction is done from `data/raw-source.html` — the HTML source fetched in Plan 01. Analyze the HTML structure and JS code within this file to reconstruct the DOM.

    **Step 1 — Extract the static HTML shell:**
    Read `raw-source.html` and identify the static HTML structure (elements that exist in the HTML, NOT created dynamically by JS). Search for:
    - `<aside id="sidebar">` — the sidebar navigation container
    - `<header>` — the sticky header
    - `<main id="content">` — the content area where views render
    - Modal container: look for `<div id="modal">` or `<div class="modal">` or similar overlay container
    - Toast container: look for `<div id="toast">` or `<div class="toast-container">` or similar fixed-position container

    Document each shell element in RECON.md §2.1:
    ```markdown
    ## 2. DOM Tree &amp; Component Hierarchy

    ### 2.1 Static Shell (persiste em todas as rotas)
    **Container raiz:** `<body class="...">` — document body classes (dark theme, font, etc.)

    #### 2.1.1 Sidebar (`<aside id="sidebar">`)
    **Elemento:** `<aside id="sidebar" class="...">`
    **Hierarquia de filhos:** [indented tree of child elements]
    **Atributos data-*:** [all data-route, data-* values]
    **Classes CSS:** [all classes on this element and children]
    **Event handlers (inferred from JS):** [which functions are called on click, from JS analysis]
    ```

    **Step 2 — Document the sidebar in detail:**
    The sidebar has 3 categories per FEATURES.md: FLUXO PRINCIPAL, DADOS, SISTEMA.
    For EACH nav-link, document:
    ```markdown
    #### Nav-Link: Etapa 1
    - **Element:** `<a class="nav-link" data-route="etapa1">`
    - **Icon:** `<span class="icon">🧪</span>` (exact emoji)
    - **Label:** `<span class="label">Etapa 1</span>`
    - **Active state:** `.active` class added by `go()` when current route matches
    - **Click handler:** `go('etapa1')` (confirmed from addEventListener or onclick in JS)
    - **Computed styles (reconstructed):** padding, border-radius, transition, etc.
    ```
    Do this for ALL navigation links across all 3 categories.

    **Step 3 — Document the sticky header:**
    ```markdown
    #### 2.1.2 Header (`<header class="sticky ...">`)
    - **Element:** `<header class="...">`
    - **Blur effect:** `backdrop-filter: blur(12px)` (confirmed from CSS)
    - **Title element:** `<h1 id="header-title">` — updated by go()
    - **Subtitle element:** `<span id="header-subtitle">` — updated by go()
    - **API status pills:** Cloudflare pill, SMS24h pill — document DOM structure, OK/Danger states
    - **Dynamic content:** title and subtitle change on route navigation
    ```

    **Step 4 — Document modal system:**
    Search JS source for `openModal()` / `closeModal()` functions to understand the modal DOM:
    - Is the modal container in static HTML or created dynamically?
    - Document the overlay/backdrop element
    - Document the modal body element
    - Document close behavior (backdrop click, X button, Escape key?)
    - Document any CSS classes applied to modal

    **Step 5 — Document toast system:**
    Search JS source for `toast()` function to understand the toast DOM:
    - Is the toast container in static HTML or created dynamically?
    - Document a single toast element structure: icon span + message span
    - Document positioning: fixed, bottom-center
    - Document auto-dismiss: setTimeout(3000)
    - Document CSS classes: glass styling

    **Step 6 — Document any other persistent UI elements:**
    Search for: loading spinners, confirmation dialogs, dropdown menus, tooltips.
    If any exist as persistent DOM elements (not created on-the-fly), document them here.
  </action>
  <verify>
    <automated>
      # Verify static shell sections exist
      grep -c "^### 2\.1" .planning/phases/01-reconnaissance-extraction/RECON.md

      # Verify sidebar documented with nav-links
      grep -c "nav-link\|data-route" .planning/phases/01-reconnaissance-extraction/RECON.md

      # Verify header documented
      grep -c "sticky\|header-title\|header-subtitle" .planning/phases/01-reconnaissance-extraction/RECON.md
    </automated>
  </verify>
  <acceptance_criteria>
    - C1: Sidebar element fully documented: parent `<aside>`, all child nav-links, 3 category groupings
    - C2: Every nav-link has: HTML element tag, data-route attribute, icon (exact emoji), label text, active state mechanism, click handler function name
    - C3: Header fully documented: sticky/blur CSS, title element ID, subtitle element ID, API status pill structure
    - C4: Modal system documented: container element (static or dynamic), overlay, body, close triggers
    - C5: Toast system documented: container, individual toast structure (icon + message), positioning, auto-dismiss timing
    - C6: All event handler connections traced from JS source (not guessed)
    - C7: No "probably" — every attribute, class, and handler is CONFIRMED from source
  </acceptance_criteria>
  <done>RECON.md §2.1 contains complete static shell documentation: sidebar (all nav-links with data-route + icons + handlers), header (title/subtitle/pills/modal/toast containers).</done>
</task>

<task type="auto">
  <name>Task 01-02-02: Per-View DOM Extraction — All 8 Views in All States</name>
  <files>.planning/phases/01-reconnaissance-extraction/RECON.md</files>
  <read_first>
    - .planning/phases/01-reconnaissance-extraction/RECON.md §2.1 (static shell)
    - .planning/phases/01-reconnaissance-extraction/RECON.md §1 (state model — needed to understand conditional elements)
    - .planning/phases/01-reconnaissance-extraction/data/raw-source.html
    - .planning/phases/01-reconnaissance-extraction/01-RESEARCH.md § Common Pitfalls — Pitfall 1: DOM Extraction Without Context
    - .planning/research/FEATURES.md § Table Stakes (each view's feature description)
  </read_first>
  <action>
    Per D-01 (layer-organized: DOM section covers ALL views from this perspective) and Pitfall 1 prevention (document ALL states, not just one):

    **Step 1 — Extract view rendering code from JS source:**
    Each view is a function in `VIEWS` that RETURNS an HTML string. Search `raw-source.html` for:
    - `VIEWS.dashboard` / `VIEWS = {` — the VIEWS registry definition
    - Each view function body: find the template literal or string concatenation that builds the HTML
    - The HTML string returned by each view function IS the DOM structure for that view

    The approach: For each of the 8 views, read the view function's source code, extract the HTML template, and document the element hierarchy.

    **Step 2 — Document Dashboard (simplest view, starts here):**
    The Dashboard view function returns HTML with:
    - Hero card (top section with welcome message)
    - 4 KPI stat cards: Empresas, Sites Criados, No Ar, Finalizados
    - 6 quick-action cards
    - API status warning (conditional)

    For EACH element, document in RECON.md §2.2:
    ```markdown
    ### 2.2 Dashboard View
    **View Function:** `VIEWS.dashboard`
    **Fonte:** [line range in source]
    **Route:** dashboard

    #### KPI Stat Card: Empresas
    - **Wrapper:** `<div class="stat-card glass">`
    - **Icon cube:** `<div class="icon-cube ...">🏢</div>` — icon-cube variant color
    - **Number:** `<span class="stat-number">` — value from `getDB().empresas.length`
    - **Label:** `<span class="stat-label">Empresas</span>`
    - **Conditional:** Always rendered (not conditional)
    ```
    Do this for EACH element in the dashboard.

    **Step 3 — Document Etapa 1 (5-step wizard — MOST COMPLEX DOM):**
    The wizard has 5 step states. The view function likely renders ALL steps but uses CSS `.disabled` or `display: none` to show/hide based on `etapa1State.currentStep`.

    For EACH step, document:
    - Step container element (`.step-card` or similar)
    - Step header (number, title, status indicator)
    - Step body (step-specific form fields, buttons, outputs)
    - Conditional visibility: which state value shows/hides this step
    - Progressive unlocking: which step unlocks which next step

    Step-specific elements to document:
    - Step 1 (CNPJ): CNPJ input field, "Buscar" button, loading state, error state, company data display
    - Step 2 (Domínio): domain suggestions list, selected domain, custom domain input
    - Step 3 (Meta Tag): generated meta-tag display, copy button
    - Step 4 (Gerar Site): site preview iframe/textarea, generated HTML display
    - Step 5 (Publicar): deploy status, progress indicators, deployment URL output

    **Step 4 — Document remaining 6 views:**
    For each view, extract from its `VIEWS[name]` function:
    - Etapa 2: country select, service select, "Comprar Número" button, phone display, SMS code input, polling status
    - Etapa 3: PDF drop zone, canvas container (multi-page), overlay elements (draggable divs with contentEditable), field mapping display, download button
    - Banco de Empresas: search input, filter dropdown, company card grid, each card's structure (name, CNPJ, porte pill, capital, "Usar" button)
    - Planilha: 8-column table (Empresa, CNPJ, Domínio/URL, Tel Empresa, Tel Nosso, Status, Atualizado, Ações), select dropdowns, delete buttons, export button
    - Configurações: Cloudflare token input, auto-detect button, account picker, SMS24h key input, test buttons, backup download, restore upload
    - Ajuda: help sections as ordered lists with icons, each guide's steps

    Document each in its own subsection (§2.3 through §2.9).

    **Step 5 — Document conditional element visibility (Pitfall 1 prevention):**
    For EVERY element that is conditionally shown/hidden, document:
    - Which state variable controls visibility
    - What value shows it
    - What value hides it
    - What CSS mechanism (`.hidden`, `display:none`, `.disabled`)

    Create a summary table in §2.10:
    ```markdown
    ### 2.10 Conditional Element Index
    | View | Elemento | Condição de Exibição | Estado Controlador | Mecanismo CSS |
    |------|----------|---------------------|-------------------|---------------|
    | Etapa 1 | Step 2 card | etapa1State.currentStep >= 2 | etapa1State | .disabled → display:none |
    ...
    ```
  </action>
  <verify>
    <automated>
      # Verify all 8 views have subsections
      test $(grep -c "^### 2\.[2-9]" .planning/phases/01-reconnaissance-extraction/RECON.md) -ge 8

      # Verify conditional element index exists
      grep -c "Condicional\|conditional" .planning/phases/01-reconnaissance-extraction/RECON.md
    </automated>
  </verify>
  <acceptance_criteria>
    - C1: All 8 views have dedicated subsections in RECON.md §2 (2.2 through 2.9)
    - C2: Each view subsection documents the complete element hierarchy extracted from the view function's HTML template
    - C3: Dashboard: 4 KPI stat cards + hero card + 6 quick-action cards fully documented
    - C4: Etapa 1: All 5 step states documented with element structure for each step + conditional visibility rules
    - C5: Etapa 2: All interactive elements (selects, buttons, phone display, SMS code input, polling status) documented
    - C6: Etapa 3: PDF container, canvas elements, overlay structure (draggable contentEditable divs), field mapping UI documented
    - C7: Banco: Card grid structure, search/filter UI, "Usar na Etapa 1" button documented
    - C8: Planilha: All 8 column headers, select dropdowns, delete buttons, export button documented
    - C9: Config: Token inputs, account picker, test buttons, backup/restore UI documented
    - C10: Ajuda: All help guide sections and step lists documented
    - C11: Conditional Element Index (§2.10) lists ≥10 conditional elements across views
    - C12: Every element's event handlers traced from JS source (not guessed from HTML alone)
  </acceptance_criteria>
  <done>RECON.md §2.2-2.9 contains complete DOM documentation for all 8 views with all state variants. §2.10 conditional index enables downstream executors to understand element lifecycle.</done>
</task>

<task type="auto">
  <name>Task 01-02-03: Route System &amp; Navigation Extraction</name>
  <files>.planning/phases/01-reconnaissance-extraction/RECON.md</files>
  <read_first>
    - .planning/phases/01-reconnaissance-extraction/RECON.md §2.1 (sidebar nav-links with data-route attributes)
    - .planning/phases/01-reconnaissance-extraction/RECON.md §2.2-2.9 (view DOM trees)
    - .planning/phases/01-reconnaissance-extraction/data/raw-source.html
    - .planning/phases/01-reconnaissance-extraction/01-RESEARCH.md § Architecture Patterns — Pattern 4: Route & Navigation System Extraction
    - .planning/research/FEATURES.md § Table Stakes — SPA Router row
  </read_first>
  <action>
    Per D-02 (complete depth — every route, every VIEWS entry, every go() side effect):

    Prepare RECON.md §3 header: `## 3. Rotas & Sistema de Navegação`.

    **Step 1 — Extract the ROUTES array from JS source:**
    Search `raw-source.html` for `ROUTES` or `const ROUTES = [` or similar. The ROUTES array defines all 8 routes. For each route object, extract EXACT values:
    - `path`: the route identifier string (e.g., 'dashboard', 'etapa1')
    - `title`: the page title string (e.g., 'Dashboard')
    - `subtitle`: the page subtitle string (e.g., 'Visão geral')
    - `view`: the VIEWS property name (e.g., 'dashboard')
    - Any other fields (icon, category, etc.)

    Document in RECON.md §3.1 as a markdown table:
    ```markdown
    ### 3.1 Tabela de Rotas (ROUTES array)
    | path | title | subtitle | view | Categoria |
    |------|-------|----------|------|-----------|
    | dashboard | Dashboard | Visão geral | dashboard | FLUXO PRINCIPAL |
    ...
    ```

    **Step 2 — Extract the VIEWS registry:**
    Search for `VIEWS = {` or `const VIEWS = {`. The VIEWS object maps view names to view functions. Document:
    - Every key in VIEWS (all 8 view names)
    - Each value: function name (e.g., `VIEWS.dashboard` references which function?)

    Document in RECON.md §3.2:
    ```markdown
    ### 3.2 VIEWS Registry
    | Chave | Função | Descrição | Fonte (linha) |
    |-------|--------|-----------|--------------|
    | dashboard | dashboard() / VIEWS.dashboard | Renderiza dashboard HTML | ~linha X |
    ...
    ```

    **Step 3 — Extract and document the go() function (THE critical function):**
    Search for `function go(` or `const go =`. The go() function is the SPA router. Document EVERYTHING:
    - **Signature:** `go(routeName: string): void`
    - **Full implementation trace** (step by step, what it does):
      1. Look up route in ROUTES array (by path matching)
      2. Update `document.title` to `${route.title} — Laboratório de BMs` or similar
      3. Update `#header-title` innerText to route.title
      4. Update `#header-subtitle` innerText to route.subtitle
      5. Toggle `.active` class on nav-links: remove from all, add to `[data-route="${route.path}"]`
      6. Call `VIEWS[route.view]()` to get HTML string
      7. Set `#content.innerHTML` to the returned HTML
      8. Call `refreshHeaderStatus()` (updates API status pills)
      9. Any history.pushState / replaceState? (hash-free routing)
      10. Any scroll-to-top?
    - **Edge cases:** What happens with invalid route? Default route?

    Document in RECON.md §3.3 with FULL pseudocode trace.

    **Step 4 — Document navigation trigger points:**
    Search for ALL places that call `go()`:
    - Sidebar nav-link click handlers
    - "Usar na Etapa 1" button in Banco de Empresas
    - Any in-view navigation links
    - Initial route on page load

    Document the complete navigation flow in §3.4:
    ```markdown
    ### 3.4 Fluxo de Navegação
    **Trigger points:** [list all places that call go()]
    **Initial route:** [which route loads on page start — likely go('dashboard')]
    **History behavior:** [whether pushState is used, back button support]
    **Side effects per navigation:**
    - title update ✓
    - header update ✓
    - nav-link.active toggle ✓
    - content swap ✓
    - API status refresh ✓
    - [any others]
    ```

    **Step 5 — Document the VIEWS function signature pattern:**
    Every view function follows the same pattern: `() => HTML_string`. Document this contract:
    - Input: no parameters (views read from global state/localStorage)
    - Output: HTML string (injected via innerHTML)
    - Side effects: none during render (event listeners attached after innerHTML, not during string generation)

    **Step 6 — Cross-reference against §2 DOM:**
    Verify every `data-route` in §2.1 sidebar has a matching entry in §3.1 ROUTES table.
    Verify every VIEWS key in §3.2 has a corresponding view function documented in §2.
    Note any discrepancies discovered.
  </action>
  <verify>
    <automated>
      # Verify ROUTES table has 8 entries
      test $(grep -c "|.*|.*|.*|.*|" .planning/phases/01-reconnaissance-extraction/RECON.md | head -1) -ge 8

      # Verify go() function documented
      grep -c "go(" .planning/phases/01-reconnaissance-extraction/RECON.md

      # Verify VIEWS documented
      grep -c "VIEWS" .planning/phases/01-reconnaissance-extraction/RECON.md
    </automated>
  </verify>
  <acceptance_criteria>
    - C1: ROUTES array fully extracted with all 8 routes, each showing: path, title, subtitle, view name, category
    - C2: VIEWS registry fully extracted with all 8 entries, each showing: key, function name, description
    - C3: go() function fully documented with step-by-step implementation trace (≥8 discrete steps)
    - C4: go() edge cases documented: invalid route handling, default route, duplicate navigation guard
    - C5: All go() call sites documented (>10 trigger points across sidebar + views)
    - C6: Navigation flow documented: initial route, history mechanism, side effect order
    - C7: data-route attributes in §2.1 all match ROUTES.path values in §3.1 (cross-verified)
    - C8: Every VIEWS key maps to a documented view function
    - C9: No "probably" or "likely" — all route data extracted from source code
  </acceptance_criteria>
  <done>RECON.md §3 contains complete route system documentation: ROUTES array, VIEWS registry, go() function with full implementation trace, navigation flow, and cross-reference to §2 DOM elements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
N/A — Documentation-only phase. No code written, no packages installed.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | RECON.md (sensitive system internals) | accept | The target system is publicly accessible. Documenting its DOM structure and routing logic does not expose secrets — API tokens are NOT documented in RECON.md. |
| T-02-02 | Tampering | RECON.md DOM documentation accuracy | mitigate | All DOM structures are extracted from the source code's HTML template strings (VIEWS functions). Cross-reference in Plan 05 verifies every documented element appears in source. |
</threat_model>

<verification>
### Cross-Reference Checks (for Plan 05)
- [ ] Every data-route in §2.1 matches a ROUTES.path in §3.1
- [ ] Every VIEWS key in §3.2 has a view function documented in §2
- [ ] All 8 route paths present in both ROUTES array and sidebar nav-links
</verification>

<success_criteria>
## Plan-Level Success
1. RECON.md §2 exists with ≥300 lines covering static shell + all 8 view DOM trees
2. Every view documented in all relevant states (not just happy path)
3. Conditional Element Index (§2.10) lists ≥10 conditionally-rendered elements
4. RECON.md §3 exists with ≥150 lines covering ROUTES, VIEWS, go(), and navigation flow
5. go() function implementation trace has ≥8 discrete steps
6. All data-route attributes cross-referenced between §2 and §3
7. Every claim backed by source code analysis (line references where possible)
</success_criteria>

<output>
Create `.planning/phases/01-reconnaissance-extraction/01-02-SUMMARY.md` when done
</output>
