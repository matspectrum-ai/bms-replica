---
phase: 01-reconnaissance-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-reconnaissance-extraction/RECON.md
  - .planning/phases/01-reconnaissance-extraction/data/localStorage-snapshot.json
  - .planning/phases/01-reconnaissance-extraction/data/lighthouse-baseline.json
autonomous: true
requirements: [RECON-03]
user_setup: []

must_haves:
  truths:
    - "Every localStorage key (lab_bms_db_v1, lab_bms_settings_v1) is documented with complete JSON schema including all nested objects and arrays"
    - "All conditional sub-objects (empresas[].raw, sites[].deploymentId, sites[].phoneNumber) are identified with exact creation triggers documented"
    - "All 3 in-memory state objects (etapa1State, etapa2State, pdfState) are catalogued with field names, types, default values, and mutation points"
    - "Lighthouse performance baseline (Navigation audit, Desktop) is captured for VAL-04 bundle size comparison"
    - "The vanilla JS architecture is confirmed via Wappalyzer + Sources panel inspection"
  artifacts:
    - path: ".planning/phases/01-reconnaissance-extraction/RECON.md"
      provides: "Section 1: localStorage & State Schema — the foundation data model for all downstream phases"
      contains: "lab_bms_db_v1 schema, lab_bms_settings_v1 schema, conditional branches table, in-memory state objects, sessionStorage inventory"
    - path: ".planning/phases/01-reconnaissance-extraction/data/localStorage-snapshot.json"
      provides: "Raw localStorage dump at initial inspection for archival reference"
    - path: ".planning/phases/01-reconnaissance-extraction/data/lighthouse-baseline.json"
      provides: "Lighthouse Navigation audit (Performance, Best Practices, Accessibility, SEO) for VAL-04 comparison"
  key_links:
    - from: "RECON.md §1.1 lab_bms_db_v1 schema"
      to: "FEATURES.md § Original System Inventory Summary (empresas[], sites[] structures)"
      via: "Cross-reference: every FEATURES.md documented field appears in RECON.md schema"
    - from: "RECON.md §1.3 In-Memory State"
      to: "RECON.md §5 (future Plan 03) Business Logic Functions"
      via: "State consumers listed so downstream executors can trace data flow"
    - from: "RECON.md §1.1 conditional branches"
      to: "RECON.md §4 (future Plan 03) API Contracts"
      via: "Conditional fields annotated with which API call creates them (e.g., deploymentId ← Cloudflare deploy)"

security_enforcement: false
---

<objective>
Extract and document the complete client-side state layer of the original system: localStorage keys and schemas, in-memory state objects, and sessionStorage — forming the foundational data model that all downstream RECON extraction depends on.

Purpose: Per RESEARCH.md Pattern 2, localStorage is THE foundation — every data-driven view reads from it. Extracting it first gives all subsequent plans (DOM, Routes, APIs, Functions) the data model context they need to be meaningful.

Output: RECON.md §1 (localStorage & State Schema, ~200+ lines), plus raw localStorage snapshot and Lighthouse baseline JSON files.
</objective>

<execution_context>
@/home/matspectrum-ai/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/matspectrum-ai/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-reconnaissance-extraction/01-CONTEXT.md
@.planning/phases/01-reconnaissance-extraction/01-RESEARCH.md
@.planning/research/FEATURES.md
@.planning/research/SUMMARY.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 01-01-01: Environment Prep, Stack Confirmation &amp; Lighthouse Baseline</name>
  <files>
    .planning/phases/01-reconnaissance-extraction/data/lighthouse-baseline.json
    .planning/phases/01-reconnaissance-extraction/RECON.md
  </files>
  <read_first>
    - .planning/phases/01-reconnaissance-extraction/01-RESEARCH.md § Standard Stack (Chrome DevTools + Wappalyzer + Lighthouse)
    - .planning/phases/01-reconnaissance-extraction/01-RESEARCH.md § Architecture Patterns — STEP 0: ENVIRONMENT PREP
    - .planning/phases/01-reconnaissance-extraction/01-RESEARCH.md § Code Examples — Lighthouse Baseline Capture
    - .planning/research/FEATURES.md § Original System: Feature Inventory Summary (for stack expectations)
  </read_first>
  <action>
    Per D-03 (100% manual DevTools extraction):

    **Step 1 — Fetch and save the raw source:**
    Use `webfetch` with format=html to retrieve `https://laboratoriodebms.netlify.app/`. Save the raw response as `.planning/phases/01-reconnaissance-extraction/data/raw-source.html` for archival. This is the single source of truth for ALL subsequent extraction tasks — every other task reads from this file.

    **Step 2 — Confirm stack via source inspection (no Wappalyzer needed):**
    Inspect the fetched HTML source for:
    - Framework detection: Search for React/Vue/Angular signatures (`__REACT_`, `__vue__`, `ng-` attributes). Confirm absence — this is vanilla JS.
    - Tailwind CSS CDN: Find the `<script>` or `<link>` tag loading Tailwind. Document the EXACT CDN URL and version (e.g., `https://cdn.tailwindcss.com` or versioned URL). This goes in RECON.md §6.6 stub (CSS section will be filled by Plan 04).
    - JS bundle: Identify inline `<script>` blocks vs external files. The original is known to be ~2135 lines of inline JS. Confirm whether it's one block or split.
    - CDN libraries: Search for pdf.js, pdf-lib, `@noble/hashes` CDN URLs. Document exact URLs and versions.
    - Google Fonts: Find any `<link>` loading Inter, Sora, or other fonts.

    **Step 3 — Initial localStorage inspection via JS source analysis:**
    Since the executor cannot actually open Chrome DevTools, analyze the JS source for all `localStorage` references:
    - Search for `localStorage.getItem`, `localStorage.setItem`, `STORAGE_KEY`, `SETTINGS_KEY` assignments.
    - Identify ALL key constant names (expected: `lab_bms_db_v1`, `lab_bms_settings_v1`).
    - Identify `getDB()`, `saveDB()`, `getSettings()`, `saveSettings()` function signatures.
    - Note any `localStorage.clear()` or `localStorage.removeItem()` calls.

    **Step 4 — Lighthouse baseline (skip if Chrome unavailable; flag for manual):**
    If `google-chrome` or `chromium` is available via CLI, run a Lighthouse audit:
    ```bash
    # Attempt if Chrome available
    which google-chrome && echo "Chrome available" || echo "Chrome not available — Lighthouse baseline must be captured manually"
    ```
    If Chrome is NOT available, document the exact Lighthouse command to run and flag as `<manual-step>` in RECON.md §7 Appendix. The exact metrics needed: Total resource size, JS resource size, CSS resource size, total requests. This is required for VAL-04 bundle size comparison in Phase 4.

    **Step 5 — Create RECON.md scaffold:**
    Initialize `.planning/phases/01-reconnaissance-extraction/RECON.md` with:
    - Title: `# RECON.md: Laboratório de BMs — Especificação de Engenharia Reversa`
    - Metadata block: extraction date, target URL, tooling used, confidence level
    - Placeholder section headers for all 7 layers per RESEARCH.md recommended structure (these will be filled by subsequent plans)
    - Fill §6.6 stub with Tailwind CDN version detected in Step 2
    - Begin §1 header: `## 1. localStorage &amp; State Schema`
  </action>
  <verify>
    <automated>
      # Verify raw source file exists and is non-trivial
      test -f .planning/phases/01-reconnaissance-extraction/data/raw-source.html
      test $(wc -c &lt; .planning/phases/01-reconnaissance-extraction/data/raw-source.html) -gt 10000

      # Verify RECON.md scaffold exists with required sections
      grep -c "^## 1\." .planning/phases/01-reconnaissance-extraction/RECON.md
      grep -c "^## 7\." .planning/phases/01-reconnaissance-extraction/RECON.md
    </automated>
  </verify>
  <acceptance_criteria>
    - C1: `raw-source.html` exists and exceeds 10 KB (proves we fetched the full app source, not an error page)
    - C2: Stack confirmed as vanilla JS (no React/Vue/Angular signatures found in source)
    - C3: Tailwind CDN URL and version documented in RECON.md §6.6 stub
    - C4: All CDN library URLs (pdf.js, pdf-lib, noble-hashes, Google Fonts) captured and documented
    - C5: All localStorage key constant names identified from JS source
    - C6: RECON.md scaffold created with all 7 section headers + metadata block
    - C7: Lighthouse baseline either captured as JSON OR documented with manual-step instructions
  </acceptance_criteria>
  <done>RECON.md scaffold exists with metadata, stack confirmation, and §6.6 Tailwind CDN stub. Raw source archived. Lighthouse baseline captured or manual steps documented.</done>
</task>

<task type="auto">
  <name>Task 01-01-02: localStorage Schema Extraction — Keys, JSON Schemas &amp; Conditional Branches</name>
  <files>
    .planning/phases/01-reconnaissance-extraction/RECON.md
    .planning/phases/01-reconnaissance-extraction/data/localStorage-snapshot.json
  </files>
  <read_first>
    - .planning/phases/01-reconnaissance-extraction/RECON.md (current state — scaffold + stack confirmation)
    - .planning/phases/01-reconnaissance-extraction/data/raw-source.html (single source of truth)
    - .planning/phases/01-reconnaissance-extraction/01-RESEARCH.md § Architecture Patterns — Pattern 2: localStorage Schema Extraction
    - .planning/phases/01-reconnaissance-extraction/01-RESEARCH.md § Common Pitfalls — Pitfall 2: Missing Conditional localStorage Branches
    - .planning/research/FEATURES.md § Original System: Feature Inventory Summary (empresas[], sites[] structures)
  </read_first>
  <action>
    Per D-02 (complete depth — every conditional branch documented):

    **Step 1 — Extract `lab_bms_db_v1` schema from JS source:**
    Read `data/raw-source.html` and locate all code that reads from or writes to `lab_bms_db_v1`. Trace:
    - `getDB()` function: find its implementation. It likely does `JSON.parse(localStorage.getItem('lab_bms_db_v1')) || DEFAULT`.
    - The DEFAULT/fallback object: extract the EXACT default structure. This IS the schema skeleton.
    - `saveDB(db)` function: find its implementation. It likely does `localStorage.setItem('lab_bms_db_v1', JSON.stringify(db))`.
    - Trace ALL code paths that mutate `db.empresas[]` or `db.sites[]` — every `.push()`, property assignment, object spread.

    **Step 2 — Document `lab_bms_db_v1` JSON schema:**
    Create RECON.md §1.1 with the complete schema. For each top-level key and nested object, document:
    ```markdown
    ### 1.1 Chave: lab_bms_db_v1
    **Storage Key:** `lab_bms_db_v1`
    **Accessors:** `getDB()` (read), `saveDB(db)` (write)
    **Default Value:** [exact fallback object from getDB()]

    #### db.empresas[]
    | Campo | Tipo | Obrigatório | Condicional? | Criado Por |
    |-------|------|-------------|-------------|------------|
    | cnpj | string (##.###.###/####-##) | Sim | Não | e1Buscar() → salvarEmpresa() |
    | razao_social | string | Sim | Não | BrasilAPI response |
    | raw | object | Não | SIM — apenas após CNPJ lookup bem-sucedido | e1Buscar() → normalizarBrasilAPI() |
    ...
    ```

    **Step 3 — Extract `lab_bms_settings_v1` schema:**
    Same process as Step 1-2 but for the settings key. Trace:
    - `getSettings()` / `saveSettings()` functions
    - All settings fields: Cloudflare tokens (single + multi-account), SMS24h API key
    - Auto-detection function (`autoConectarTokens`): what it writes
    - Backup/restore functions: what format they export/import

    **Step 4 — Identify ALL conditional branches (CRITICAL per Pitfall 2):**
    Search the JS source for ALL code paths that conditionally create or populate object properties. For each:
    - `empresas[].raw` — only populated by `normalizarBrasilAPI()` after successful CNPJ lookup. Document the EXACT fields inside `raw` (cnae_fiscal_descricao, municipio, uf, etc.)
    - `sites[].deploymentId` — only after Cloudflare deploy succeeds. Find which function sets it (`e1Publicar()` success branch).
    - `sites[].phoneNumber` — only after Etapa 2 SMS purchase. Find which function sets it (`atualizarSiteComNumero()`).
    - Any other conditional keys: search for patterns like `obj.key = value` inside `if` blocks, or optional chaining operators.

    For EACH conditional branch, document in a table:
    | Campo | Objeto Pai | Condição de Criação | Função Responsável | Valor Típico |
    |-------|-----------|---------------------|-------------------|-------------|

    **Step 5 — Document localStorage write sequence (mutation trace):**
    Monkey-patch simulation: trace through the source code to document the COMPLETE sequence of localStorage writes during key workflows. For the Etapa 1 workflow (CNPJ → deploy):
    1. Initial state (empty or existing)
    2. After CNPJ lookup: `db.empresas` gains new entry (with `raw`)
    3. After site generation: `db.sites` gains new entry (status: "criado")
    4. After Cloudflare deploy: `db.sites[].deploymentId` populated, status → "no_ar"

    Document this as a sequential trace in RECON.md §1.1.

    **Step 6 — Save raw snapshot:**
    Create `data/localStorage-snapshot.json` containing the reconstructed DEFAULT state object (what a fresh install would have). This serves as the canonical reference for Phase 2's persistence layer initialization.
  </action>
  <verify>
    <automated>
      # Verify RECON.md §1.1 exists and has substantial content
      grep -c "### 1.1" .planning/phases/01-reconnaissance-extraction/RECON.md
      test $(grep -c "lab_bms_db_v1\|lab_bms_settings_v1" .planning/phases/01-reconnaissance-extraction/RECON.md) -ge 2

      # Verify conditional branches table exists
      grep -c "CONDICIONAL\|Condicional\|condicional" .planning/phases/01-reconnaissance-extraction/RECON.md

      # Verify the words "empresas" and "sites" both appear (core data structures)
      grep -c "empresas" .planning/phases/01-reconnaissance-extraction/RECON.md
      grep -c "sites" .planning/phases/01-reconnaissance-extraction/RECON.md
    </automated>
  </verify>
  <acceptance_criteria>
    - C1: `lab_bms_db_v1` schema documented with ALL top-level keys (empresas, sites, etc.) and their nested fields
    - C2: Every field in the schema has: data type, required/optional, conditional flag, and "created by" annotation
    - C3: `lab_bms_settings_v1` schema documented with Cloudflare tokens, SMS24h key, and any other fields
    - C4: Conditional branches table exists with ≥3 documented entries (raw, deploymentId, phoneNumber minimum)
    - C5: Each conditional branch documents: parent object, creation condition, responsible function, typical value
    - C6: localStorage write sequence trace documented for at least the Etapa 1 workflow
    - C7: No field is documented as "probably" or "maybe" — every field is CONFIRMED from source code analysis
    - C8: `localStorage-snapshot.json` contains the reconstructed DEFAULT state
  </acceptance_criteria>
  <done>RECON.md §1.1 and §1.2 contain complete localStorage schemas with all conditional branches annotated. Raw snapshot saved. Every downstream plan now knows the exact data model.</done>
</task>

<task type="auto">
  <name>Task 01-01-03: In-Memory State Objects &amp; Session Storage Documentation</name>
  <files>
    .planning/phases/01-reconnaissance-extraction/RECON.md
  </files>
  <read_first>
    - .planning/phases/01-reconnaissance-extraction/RECON.md §1.1-1.2 (localStorage schemas from Task 01-01-02)
    - .planning/phases/01-reconnaissance-extraction/data/raw-source.html
    - .planning/research/FEATURES.md § Original System: Feature Inventory Summary (etapa1State, etapa2State, pdfState)
    - .planning/phases/01-reconnaissance-extraction/01-RESEARCH.md § Architecture Patterns — Pattern 5 (Scope pane inspection)
  </read_first>
  <action>
    Per D-02 (complete depth — every state object documented with fields, defaults, mutations):

    **Step 1 — Extract etapa1State from JS source:**
    Search the raw source for `etapa1State` (or similar variable name that holds wizard state). Document:
    - Declaration location and initial value (all 5 fields per FEATURES.md)
    - Each field: name, type, default value, what user action changes it
    - All 5 step states and how `etapa1State.currentStep` transitions between them
    - Progressive unlocking logic: what condition gates each step's enablement
    - Reset behavior: when/how etapa1State is cleared (e.g., new wizard session)

    Document in RECON.md §1.3.1:
    ```markdown
    ### 1.3.1 etapa1State
    **Initialization:** `let etapa1State = { ... }` at [line ~X in source]
    **Scope:** Module-level (accessible to all Etapa 1 functions)
    **Reset Trigger:** [function name / user action]

    | Campo | Tipo | Default | Mutado Por | Descrição |
    |-------|------|---------|-----------|-----------|
    | currentStep | number | 1 | e1Buscar(), e1Gerar(), e1Publicar() | 1-5, controls wizard step visibility |
    | cnpjData | object|null | null | e1Buscar() success | Raw BrasilAPI response |
    ...
    ```

    **Step 2 — Extract etapa2State from JS source:**
    Search for `etapa2State`. Document all 4 fields (per FEATURES.md):
    - SMS purchase state: selected country, selected service, purchased number, activation code
    - Auto-polling state: interval ID, poll attempts, timeout logic
    - Which functions read each field, which functions write each field

    Document in RECON.md §1.3.2 with same table format.

    **Step 3 — Extract pdfState from JS source:**
    Search for `pdfState`. Document all 4 fields (per FEATURES.md):
    - PDF document reference, page count, current page, overlay data structure
    - Overlay data structure in detail: `{ id, text, x, y, width, height }` or similar
    - Which functions create overlays, reposition them, delete them

    Document in RECON.md §1.3.3 with same table format.

    **Step 4 — Discover any ADDITIONAL state objects (beyond the 3 known):**
    Search the JS source for module-level `let`/`var`/`const` declarations that hold mutable state:
    - UI state: sidebar toggle, active route, toast queue
    - Timer state: any `setInterval`/`setTimeout` IDs stored in variables
    - Cache state: any memoized/cached API responses or computed values
    - Event listener references: any stored event handler references for cleanup

    For each discovered object, add a subsection to §1.3.
    Cross-reference: the known state should include at minimum `currentRoute` (set by go()), `sidebarOpen` (toggled by toggleSidebar()), and any toast queue.

    **Step 5 — Document sessionStorage (if any):**
    Search source for `sessionStorage`. Most SPAs don't use it, but verify. If none found, document: "No sessionStorage usage detected" with the search patterns used.
    If found, extract schemas same as localStorage.

    **Step 6 — Document the bootstrap/initialization sequence:**
    Trace from the IIFE/bottom-of-script entry point:
    1. What runs first? (likely `autoConectarTokens()`, then `instalarProxy()`)
    2. When is `getDB()` first called?
    3. What initial route is loaded? (likely `go('dashboard')`)
    4. Are there any `DOMContentLoaded` listeners?

    Document in RECON.md §1.4 (if applicable) or as a preamble to §5.10 (to be filled by Plan 03).
  </action>
  <verify>
    <automated>
      # Verify state objects documented
      grep -c "etapa1State\|etapa2State\|pdfState" .planning/phases/01-reconnaissance-extraction/RECON.md

      # Verify at least 3 state object sections
      test $(grep -c "^### 1\.3\." .planning/phases/01-reconnaissance-extraction/RECON.md) -ge 3
    </automated>
  </verify>
  <acceptance_criteria>
    - C1: etapa1State documented with all 5+ fields, default values, mutation functions, and step transition logic
    - C2: etapa2State documented with all 4+ fields, including polling timer state
    - C3: pdfState documented with all 4+ fields, including overlay data structure (not just "contains overlays" — exact shape)
    - C4: Any ADDITIONAL state objects discovered beyond the 3 known are documented
    - C5: At minimum, UI state (currentRoute, sidebarOpen) and toast queue are accounted for
    - C6: sessionStorage verified and documented (or confirmed absent with search patterns noted)
    - C7: Bootstrap/initialization sequence documented (what runs first, in what order)
    - C8: No field is documented as "probably" or "likely" — all are CONFIRMED from source code
  </acceptance_criteria>
  <done>RECON.md §1.3 contains complete documentation of all in-memory state objects with field-level detail, mutation traceability, and bootstrap sequence. RECON.md §1 is now complete (~200+ lines).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
N/A — This is a documentation-only phase. No code is written, no packages are installed, no data is processed beyond reading the target site's publicly accessible source code.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | raw-source.html (saved target site) | accept | The target site's source is already publicly accessible at laboratoriodebms.netlify.app. Saving a local copy introduces no new exposure. |
| T-01-02 | Tampering | RECON.md documentation integrity | mitigate | All claims in RECON.md are backed by source-code citations (line references, function names). Cross-reference validation in Plan 05 verifies completeness against FEATURES.md inventory. |
</threat_model>

<verification>
### Manual Completeness Checks (for Plan 05 cross-reference)
- [ ] All localStorage accessor functions (getDB, saveDB, getSettings, saveSettings) identified
- [ ] All 3 state objects (etapa1State, etapa2State, pdfState) catalogued
- [ ] All conditional branches (≥3: raw, deploymentId, phoneNumber) documented with triggers
- [ ] Bootstrap sequence traceable from entry point to initial route render
</verification>

<success_criteria>
## Plan-Level Success
1. RECON.md §1 exists with ≥200 lines covering localStorage schemas and state objects
2. All FEATURES.md-identified state artifacts (2 localStorage keys, 3 state objects) have complete entries
3. Conditional branches table has ≥3 documented entries with creation triggers
4. `localStorage-snapshot.json` contains the reconstructed DEFAULT state
5. Bootstrap initialization sequence is documented
6. Every schema field is confirmed from source code analysis (no "probably" or "maybe")
</success_criteria>

<output>
Create `.planning/phases/01-reconnaissance-extraction/01-01-SUMMARY.md` when done
</output>
