---
phase: 01-reconnaissance-extraction
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - .planning/phases/01-reconnaissance-extraction/RECON.md
  - .planning/phases/01-reconnaissance-extraction/data/api-har-archive.json
autonomous: true
requirements: [RECON-02, RECON-05]
user_setup: []

must_haves:
  truths:
    - "Every external API endpoint (BrasilAPI CNPJ, Cloudflare Pages 5-step pipeline, SMS24h purchase/polling, Cloudflare account detection) is documented with complete request/response schemas for both success and error cases"
    - "All ~60 business logic functions are documented with signatures, parameters, return values, state mutations, and side effects"
    - "Every function's call graph is documented: Called By (callers) and Calls (callees)"
    - "API contracts include exact URLs (both proxied and upstream), HTTP methods, headers (including auth), request body schemas, response body schemas, status codes, and error response shapes"
    - "The CORS proxy layer (instalarProxy) is documented with URL rewriting rules"
    - "All formatting utilities produce expected Brazilian format outputs with documented edge cases"
  artifacts:
    - path: ".planning/phases/01-reconnaissance-extraction/RECON.md"
      provides: "Section 4: Contratos de API AND Section 5: Funções de Lógica de Negócio"
      contains: "All API contracts (4 external services), all ~60 function specifications with signatures/call-graphs/side-effects, proxy layer documentation"
    - path: ".planning/phases/01-reconnaissance-extraction/data/api-har-archive.json"
      provides: "Structured API contract archive for offline reference during Phase 2-3 development"
  key_links:
    - from: "RECON.md §4.x API endpoint"
      to: "RECON.md §1 (Plan 01) localStorage schema"
      via: "API response fields annotated with which localStorage keys they populate"
    - from: "RECON.md §5.x Business function"
      to: "RECON.md §2 (Plan 02) DOM elements"
      via: "Functions annotated with which DOM elements they read/write"
    - from: "RECON.md §5.x Business function"
      to: "RECON.md §1.3 (Plan 01) In-memory state"
      via: "Functions annotated with which state objects they mutate"
    - from: "RECON.md §4.5 CORS Proxy"
      to: "RECON.md §5.10 Boot/Proxy"
      via: "instalarProxy() function documented in both API (§4) and Logic (§5) layers"

security_enforcement: false
---

<objective>
Extract and document every external API contract and every business logic function from the original system: API endpoints with complete request/response schemas (success + error), all ~60 functions with signatures/parameters/return-values/side-effects/call-graphs, formatting utilities, and the CORS proxy layer.

Purpose: This is the behavioral specification that Phase 3 (Views & Integrations) implements. Without exact API contracts, downstream builders cannot integrate external services. Without exact function specifications, they cannot replicate behavior. Combined with §1-3 from Plans 01-02, this completes the full specification — only CSS (§6) remains.

Output: RECON.md §4 (Contratos de API, ~250+ lines), §5 (Funções de Lógica de Negócio, ~500+ lines), and `api-har-archive.json`.
</objective>

<execution_context>
@/home/matspectrum-ai/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/matspectrum-ai/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-reconnaissance-extraction/RECON.md
@.planning/phases/01-reconnaissance-extraction/data/raw-source.html
@.planning/phases/01-reconnaissance-extraction/01-RESEARCH.md
@.planning/research/FEATURES.md
@.planning/phases/01-reconnaissance-extraction/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 01-03-01: API Contract Capture — All Endpoints, Success + Error Schemas</name>
  <files>
    .planning/phases/01-reconnaissance-extraction/RECON.md
    .planning/phases/01-reconnaissance-extraction/data/api-har-archive.json
  </files>
  <read_first>
    - .planning/phases/01-reconnaissance-extraction/RECON.md §1-3 (state model, DOM, routes — needed to understand API trigger context)
    - .planning/phases/01-reconnaissance-extraction/data/raw-source.html
    - .planning/phases/01-reconnaissance-extraction/01-RESEARCH.md § Architecture Patterns — Pattern 3: API Contract Extraction from Network Tab
    - .planning/phases/01-reconnaissance-extraction/01-RESEARCH.md § Common Pitfalls — Pitfall 3: Documenting APIs Without Error Responses
    - .planning/phases/01-reconnaissance-extraction/01-RESEARCH.md § Common Pitfalls — Pitfall 6: Ignoring CORS Proxy Layer
    - .planning/research/FEATURES.md § Table Stakes (API-dependent features), § Original System Inventory Summary
  </read_first>
  <action>
    Per D-02 (complete depth — ALL API branches documented, success + error) and Pitfall 3 + 6 prevention:

    ALL extraction is from `data/raw-source.html` — analyze JS source code for fetch() calls, URL construction, request building, and response handling.

    Prepare RECON.md §4 header: `## 4. Contratos de API`.

    **Step 1 — Find ALL fetch() / XHR calls in the JS source:**
    Search `raw-source.html` for:
    - `fetch(` — all fetch API calls
    - `XMLHttpRequest` — if any XHR usage
    - URL string patterns: `brasilapi.com.br`, `api.cloudflare.com`, `cloudflare.com/client/v4`, `sms24h` or similar
    - Proxy URL patterns: `/cf-api/`, `/sms-api/`, or any path-rewriting prefix

    Create a master list of ALL API call sites with: function name, URL template, HTTP method, request body source.

    **Step 2 — Document BrasilAPI CNPJ Lookup (§4.1):**
    Find the function that calls BrasilAPI (likely `e1Buscar()` or a helper). Extract:
    ```markdown
    ### 4.1 BrasilAPI — CNPJ Lookup
    **Endpoint:** GET `https://brasilapi.com.br/api/cnpj/v1/{cnpj}`
    **Trigger:** e1Buscar() in Etapa 1, Step 1 (user clicks "Buscar" after entering CNPJ)
    **Method:** GET
    **Headers:** [list any custom headers — likely none for public API]
    **URL Construction:** How CNPJ is sanitized before being inserted into URL (onlyDigits? format?)
    **Timeout/Error Handling:** [how the code handles network errors, timeouts]

    **Success Response (200):**
    ```json
    {
      "cnpj": "string (##.###.###/####-##)",
      "razao_social": "string",
      ... [ALL fields extracted from response handling code]
    }
    ```

    **Response Normalization:** [document the normalizarBrasilAPI() function — what fields are mapped/renamed]
    **Error Response (404 / 400):** [extract error handling code — what status codes are checked, what error messages are shown to user]
    **Rate Limiting:** [any rate limit handling observed in code]
    ```

    **Step 3 — Document Cloudflare Pages API 5-Step Pipeline (§4.2):**
    This is the MOST COMPLEX API integration. Find all Cloudflare-related fetch calls. The pipeline has 5 steps:
    1. Create Pages Project — POST `/accounts/{id}/pages/projects`
    2. Get JWT — GET `/accounts/{id}/pages/projects/{name}/upload-token`
    3. BLAKE3 Hash — client-side hash of site content (not an API call, but a prerequisite for upload)
    4. Upload Asset — POST to upload URL with form data
    5. Create Deployment — POST `/accounts/{id}/pages/projects/{name}/deployments`

    For EACH step, document:
    - Exact endpoint URL (both proxied `/cf-api/...` AND upstream `api.cloudflare.com/...`)
    - HTTP method
    - Request headers (Authorization: Bearer {token}, Content-Type)
    - Request body schema (exact JSON shape)
    - Success response schema (status code, response body)
    - Error response schema (what status codes are handled, error body shape)
    - Which function orchestrates which step
    - Sequential dependency: which step's output feeds which step's input

    **Step 4 — Document Cloudflare Account Detection (§4.3):**
    Find the `autoConectarTokens()` or account detection function. Document:
    - Endpoint: GET `/accounts` or similar
    - How the token is validated
    - Multi-account response shape
    - How accounts are displayed in Configurações UI

    **Step 5 — Document SMS24h API (§4.4):**
    Find the `smsAPI()` wrapper function and all its call sites. Document:
    - Base URL and proxy rewriting
    - Authentication method (API key in header? query param?)
    - Endpoints:
      - getBalance: method, URL, response schema
      - getNumbers(country, service): method, URL, params, response schema (array of available numbers)
      - buyNumber(numberId): method, URL, request body, response schema
      - getSMS(activationId): method, URL, response schema (activation code)
    - Polling logic: interval duration, max attempts, timeout behavior
    - Error response shapes for each endpoint

    **Step 6 — Document the CORS Proxy Layer (§4.5):**
    Find `instalarProxy()` function. This monkey-patches `window.fetch` to rewrite URLs. Document:
    - Which URL patterns are rewritten (e.g., `/cf-api/*` → `https://api.cloudflare.com/client/v4/*`)
    - The exact rewrite rules (string replacement, regex, etc.)
    - Whether headers are modified
    - Netlify `_redirects` configuration (if visible in source or inferrable from proxy rules)
    - Why the proxy exists: CORS avoidance explanation

    **Step 7 — Force error documentation (Pitfall 3 prevention):**
    For EVERY endpoint, search the JS source for error handling code:
    - `.catch()` blocks
    - `if (!response.ok)` checks
    - `response.status === 404` type checks
    - `throw new Error(...)` with error messages
    - Toast calls with error messages

    Document the EXACT error response shape the code expects and how it surfaces errors to the user. If the source does NOT handle certain error codes, document that as "unhandled — will crash/behave unexpectedly."

    **Step 8 — Create API contract archive:**
    Compile all extracted API contracts into `data/api-har-archive.json` structured as:
    ```json
    {
      "extractionDate": "2026-06-27",
      "sourceUrl": "https://laboratoriodebms.netlify.app/",
      "endpoints": [
        {
          "id": "brasilapi-cnpj",
          "method": "GET",
          "url": "https://brasilapi.com.br/api/cnpj/v1/{cnpj}",
          "proxiedUrl": null,
          "successStatus": 200,
          "successBody": { ... },
          "errorStatuses": [400, 404],
          "errorBodies": { ... }
        },
        ...
      ]
    }
    ```
    This serves as the machine-readable API specification for Phase 3's service layer.
  </action>
  <verify>
    <automated>
      # Verify API contract sections exist
      grep -c "^### 4\.[1-5]" .planning/phases/01-reconnaissance-extraction/RECON.md

      # Verify success AND error cases documented
      grep -c "Success\|Sucesso\|200" .planning/phases/01-reconnaissance-extraction/RECON.md
      grep -c "Error\|Erro\|4[0-9][0-9]\|5[0-9][0-9]" .planning/phases/01-reconnaissance-extraction/RECON.md

      # Verify proxy layer documented
      grep -c "instalarProxy\|CORS.*[Pp]roxy\|_redirects" .planning/phases/01-reconnaissance-extraction/RECON.md

      # Verify HAR archive exists
      test -f .planning/phases/01-reconnaissance-extraction/data/api-har-archive.json
    </automated>
  </verify>
  <acceptance_criteria>
    - C1: BrasilAPI CNPJ endpoint fully documented: URL template, method, headers, success response schema (ALL fields), error response schemas (≥2 status codes), normalization logic
    - C2: Cloudflare Pages 5-step pipeline documented step-by-step: each of the 5 steps has complete request/response schemas, dependency order, and orchestrating function
    - C3: Cloudflare Account Detection endpoint documented with multi-account response shape
    - C4: SMS24h API fully documented: base URL, auth method, all 4+ endpoints (balance, numbers, buy, getSMS), polling logic (interval, max attempts, timeout)
    - C5: CORS Proxy Layer documented: URL rewriting rules, which patterns map to which upstream URLs, instalarProxy() mechanism
    - C6: EVERY API endpoint has BOTH success AND error response documentation (not "error: TBD")
    - C7: Error handling documented: what status codes are checked, what error messages are shown, what happens on network failure
    - C8: Proxied URLs vs upstream URLs both documented per Pitfall 6
    - C9: API contract archive JSON is machine-readable and complete
    - C10: No endpoint is "guessed" — all extracted from source code analysis
  </acceptance_criteria>
  <done>RECON.md §4 contains complete API contracts for all 4 external services with success + error schemas. CORS proxy documented. `api-har-archive.json` created.</done>
</task>

<task type="auto">
  <name>Task 01-03-02: Core, Utility &amp; Infrastructure Function Extraction</name>
  <files>.planning/phases/01-reconnaissance-extraction/RECON.md</files>
  <read_first>
    - .planning/phases/01-reconnaissance-extraction/RECON.md §1-4
    - .planning/phases/01-reconnaissance-extraction/data/raw-source.html
    - .planning/phases/01-reconnaissance-extraction/01-RESEARCH.md § Architecture Patterns — Pattern 5: Business Logic Function Extraction
    - .planning/research/FEATURES.md § Original System: Feature Inventory Summary (Core/Infra + Utils rows)
  </read_first>
  <action>
    Per D-02 (complete depth — every function with signature, params, return, side effects) and RESEARCH.md Pattern 5 (dependency-order documentation):

    Prepare RECON.md §5 header: `## 5. Funções de Lógica de Negócio`.

    Document functions in DEPENDENCY ORDER (foundation functions first — they're called by everything else):

    **Step 1 — Document Core/Infrastructure functions (§5.1):**
    These are the functions EVERY view depends on. Document first:

    For EACH function, use this template:
    ```markdown
    ### [functionName](params)
    **Source:** [line range in pretty-printed source / function location]
    **Signature:** `functionName(param1: Type, param2: Type): ReturnType`
    **Parameters:**
      - `param1` (Type): description
    **Return:** [type and description]
    **Side Effects:**
      - DOM writes: [list specific elements modified]
      - localStorage writes: [specific keys set]
      - Network: [fetch calls made]
      - Timers: [setInterval/setTimeout created]
      - Event listeners: [listeners added]
      - Global state mutations: [specific variables modified]
    **Called By:** [list of calling functions]
    **Calls:** [list of functions this function invokes]
    **Edge Cases:** [documented handling of invalid inputs, empty states, errors]
    ```

    Functions to document in §5.1 (extract ALL from source):
    - `getDB()` — localStorage read with fallback default
    - `saveDB(db)` — localStorage write with JSON serialization
    - `getSettings()` — settings read with fallback default
    - `saveSettings(settings)` — settings write
    - `go(routeName)` — SPA router (detailed in §3, summarize here with cross-reference)
    - `toast(message, icon)` — transient notification
    - `openModal(html)` — modal overlay with HTML content
    - `closeModal()` — modal dismissal
    - `toggleSidebar()` — mobile sidebar collapse
    - `copyText(text, successMessage)` — clipboard copy
    - `refreshHeaderStatus()` — API status pill updater
    - `autoConectarTokens()` — bootstrap token detection
    - `instalarProxy()` — fetch monkey-patch for CORS

    **Step 2 — Document Utility/Formatting functions (§5.9):**
    These are pure functions (no side effects) used across many views:
    - `fmtCNPJ(cnpj)` — 14-digit → ##.###.###/####-##
    - `fmtMoney(value)` — number → R$ X.XXX,XX
    - `fmtDate(dateString)` → DD/MM/AAAA
    - `formatBRPhone(phone)` — Brazilian phone format
    - `slugify(text)` — URL-friendly slug
    - `escapeHTML(str)` — XSS prevention
    - `onlyDigits(str)` — strip non-numeric characters

    For each, document: exact input format expected, exact output format, edge cases (empty string, invalid input, wrong length).

    **Step 3 — Document Bootstrap/Proxy functions (§5.10):**
    - `autoConectarTokens()` — full logic: how it detects tokens, validates them, populates settings
    - `instalarProxy()` — full logic: how it wraps fetch, rewriting rules, error handling
    - Any IIFE or initialization code at the bottom of the script

    **Step 4 — Build function call graph:**
    After documenting each function, compile a dependency matrix. For each function, verify:
    - Every function it calls is also documented
    - Every function that calls it is also documented
    - No orphan functions (documented but nothing calls them)
    - No missing callees

    Document any discrepancies as "⚠ Call graph gap" annotations.
  </action>
  <verify>
    <automated>
      # Verify core functions documented
      grep -c "^### \(getDB\|saveDB\|getSettings\|saveSettings\|go\|toast\|openModal\|closeModal\|toggleSidebar\|copyText\|refreshHeaderStatus\)" .planning/phases/01-reconnaissance-extraction/RECON.md

      # Verify formatting utils documented
      grep -c "^### \(fmtCNPJ\|fmtMoney\|fmtDate\|formatBRPhone\|slugify\|escapeHTML\|onlyDigits\)" .planning/phases/01-reconnaissance-extraction/RECON.md

      # Verify bootstrap/proxy documented
      grep -c "autoConectarTokens\|instalarProxy" .planning/phases/01-reconnaissance-extraction/RECON.md
    </automated>
  </verify>
  <acceptance_criteria>
    - C1: All 13+ core/infrastructure functions documented with full template (signature, params, return, side effects, callers, callees, edge cases)
    - C2: All 7+ formatting utilities documented with exact input/output formats and edge cases
    - C3: Bootstrap sequence documented: what runs on page load, in what order
    - C4: instalarProxy() URL rewriting rules documented with before/after examples
    - C5: Every function's "Called By" and "Calls" fields are populated (not "unknown")
    - C6: Function call graph is consistent: every callee is also documented, call chains are traceable
    - C7: Side effects are specific — "DOM writes" lists exact element IDs, not "updates the page"
  </acceptance_criteria>
  <done>RECON.md §5.1, §5.9, §5.10 contain complete documentation of all core, utility, and bootstrap functions with call graph. Foundation functions documented first.</done>
</task>

<task type="auto">
  <name>Task 01-03-03: Business Logic &amp; View Function Extraction — All ~60 Functions</name>
  <files>.planning/phases/01-reconnaissance-extraction/RECON.md</files>
  <read_first>
    - .planning/phases/01-reconnaissance-extraction/RECON.md §5.1, §5.9, §5.10 (core functions from Task 01-03-02)
    - .planning/phases/01-reconnaissance-extraction/data/raw-source.html
    - .planning/phases/01-reconnaissance-extraction/01-RESEARCH.md § Architecture Patterns — Pattern 5
    - .planning/research/FEATURES.md § Original System: Feature Inventory Summary (all view-specific rows)
    - .planning/research/FEATURES.md § Feature Prioritization Matrix
  </read_first>
  <action>
    Per D-02 (complete depth — all ~60 functions) and RESEARCH.md Pattern 5 (systematic extraction):

    Document ALL remaining business logic functions organized by view module, following the FEATURES.md inventory. For each function, use the same template as Task 01-03-02.

    **Step 1 — Document Dashboard functions (§5.2):**
    - `VIEWS.dashboard()` — renders full dashboard HTML
    - `statCard(config)` — KPI stat card factory (icon, number, label, variant)
    - `quickCard(config)` — quick-action card factory
    - Any other dashboard-specific functions discovered in source

    **Step 2 — Document Etapa 1 functions (§5.3) — LARGEST view module:**
    Functions expected (extract ALL from source, verify against FEATURES.md):
    - `VIEWS.etapa1()` — renders full wizard HTML
    - `e1Buscar()` — CNPJ lookup + response normalization
    - `e1Gerar()` — domain suggestions + meta-tag generation + site HTML generation
    - `e1Publicar()` — Cloudflare 5-step deploy pipeline orchestration
    - `gerarSugestoesDominio(nome)` — 7-algorithm domain engine
    - `normalizarBrasilAPI(response)` — BrasilAPI response normalization
    - `salvarEmpresa(dados)` — save company to localStorage
    - `stepBox(config)` — wizard step container factory
    - `buildSiteHTML(dados)` — ~300-line template engine
    - `calcAnos(data)` — years-since calculation for generated site footer
    - Any additional functions discovered in source (search for all functions in Etapa 1 code block)

    For `buildSiteHTML(dados)` SPECIFICALLY — document with extra detail:
    - All template sections (hero, about, values, services, contact, footer)
    - All CSS classes used within the generated HTML
    - Conditional sections (which data fields cause sections to appear/disappear)
    - External resource references (Google Fonts, icons, etc.)
    - The EXACT HTML structure — this is a sub-template that generates complete landing pages

    **Step 3 — Document Etapa 2 functions (§5.4):**
    - `VIEWS.etapa2()` — renders SMS purchase view
    - `smsAPI(action, params)` — SMS24h API wrapper (all action types)
    - `smsPolling(activationId)` — auto-polling for activation code
    - `atualizarSiteComNumero(siteId, phoneNumber)` — re-deploy site with new phone number
    - Any additional Etapa 2 functions discovered

    **Step 4 — Document Etapa 3 functions (§5.5):**
    - `VIEWS.etapa3()` — renders PDF editor view
    - `carregarPDF(file)` — pdf.js rendering (multi-page)
    - `rerenderOverlays()` — overlay positioning after page change / zoom
    - `baixarPDF()` — pdf-lib merge + download
    - `mapearCampos(overlays)` — map overlay positions to address fields
    - `extrairCamposEndereco(text)` — regex for 7 Brazilian address fields
    - Overlay drag handlers (if separate functions)
    - CDN library loading logic

    Document the EXACT regex patterns used in `extrairCamposEndereco()` — critical for Phase 3 clone.

    **Step 5 — Document Banco de Empresas functions (§5.6):**
    - `VIEWS.banco()` — renders company database view
    - `renderBanco()` — dynamic card grid with search/filter
    - `limparBanco()` — clear all companies
    - `usarEmpresaNaEtapa1(cnpj)` — feed company into Etapa 1 wizard
    - Search/filter logic (may be inline in renderBanco)

    **Step 6 — Document Planilha functions (§5.7):**
    - `VIEWS.planilha()` — renders spreadsheet view
    - `renderPlanilha()` — 8-column table generation
    - `mudarStatus(siteId, newStatus)` — inline status update
    - `removerSite(siteId)` — delete with confirmation
    - `exportCSV()` — CSV generation with UTF-8 BOM for Excel

    **Step 7 — Document Configurações functions (§5.8):**
    - `VIEWS.config()` — renders settings view
    - `salvarConfig()` — save all settings
    - `salvarTokenCF(token)` — Cloudflare token save + auto-detect
    - `escolherConta(accountId)` — multi-account selection
    - `testarCloudflare()` — token validation test
    - `testarSMS()` — SMS24h key validation test
    - `exportBackup()` — full JSON download
    - `importBackup(file)` — JSON upload + restore

    **Step 8 — Document Ajuda functions (§5.9 moved, or separate):**
    - `VIEWS.ajuda()` — renders help view
    - `ajuda(sectionId)` — help section renderer

    **Step 9 — Cross-reference completeness check:**
    After documenting ALL functions, cross-reference against FEATURES.md § Original System Inventory Summary:
    - Core/Infra: ~80 lines, ~9 functions → all documented?
    - Dashboard: ~50 lines, ~3 functions → all documented?
    - Etapa 1: ~350 lines, ~10 functions → all documented?
    - Etapa 2: ~200 lines, ~4 functions → all documented?
    - Etapa 3: ~200 lines, ~6 functions → all documented?
    - Banco: ~70 lines, ~4 functions → all documented?
    - Planilha: ~80 lines, ~5 functions → all documented?
    - Config: ~150 lines, ~8 functions → all documented?
    - Ajuda: ~40 lines, ~2 functions → all documented?
    - Boot/Proxy: ~40 lines, ~2 functions → all documented?
    - Utils: ~50 lines, ~7 functions → all documented?
    - Site Gen: ~300 lines, ~2 functions → all documented?

    **TOTAL expected: ~60 functions.** If count is below 55, search source for more. If above 65, that's fine — document all.

    Create a summary table in §5.11:
    ```markdown
    ### 5.11 Function Inventory Summary
    | Module | Expected (FEATURES.md) | Documented | Coverage |
    |--------|----------------------|------------|----------|
    | Core/Infra | 9 | [count] | [%] |
    ...
    | TOTAL | ~60 | [count] | [%] |
    ```
  </action>
  <verify>
    <automated>
      # Verify all view modules have function subsections
      grep -c "^### 5\.[2-9] " .planning/phases/01-reconnaissance-extraction/RECON.md

      # Count documented functions (approximate — each "### functionName" is a function doc)
      grep -c "^### [a-z]" .planning/phases/01-reconnaissance-extraction/RECON.md

      # Verify function inventory summary exists
      grep -c "Function Inventory\|Inventário" .planning/phases/01-reconnaissance-extraction/RECON.md
    </automated>
  </verify>
  <acceptance_criteria>
    - C1: All 8 view modules have function subsections in RECON.md §5
    - C2: ≥55 functions documented (target is ~60 per FEATURES.md)
    - C3: Every documented function has: signature, parameters (with types), return value (with type), side effects (specific), Called By list, Calls list
    - C4: `buildSiteHTML(dados)` documented with extra detail: all template sections, CSS classes, conditional sections
    - C5: `extrairCamposEndereco(text)` documented with EXACT regex patterns for all 7 Brazilian address fields
    - C6: `smsAPI(action, params)` documented with all action types and their corresponding endpoint/params
    - C7: Cloudflare 5-step pipeline functions: each step's orchestrating function documented with exact fetch call
    - C8: Function inventory summary table (§5.11) shows ≥90% coverage of FEATURES.md inventory
    - C9: Any function with "unknown" in Called By or Calls is flagged as ⚠ gap
    - C10: side effects are traceable — every DOM write specifies element ID, every localStorage write specifies key
  </acceptance_criteria>
  <done>RECON.md §5.2-5.11 contains complete documentation of all ~60 business logic functions with signatures, call graphs, and side effects. Cross-referenced against FEATURES.md inventory with coverage metrics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
N/A — Documentation-only phase.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | API tokens documented in RECON.md | mitigate | Do NOT include actual token values from the original. Document the token FORMAT and where they're stored, but redact any real tokens found in source code. |
| T-03-02 | Information Disclosure | API endpoint URLs and auth mechanisms documented | accept | The original system's API endpoints are already publicly discoverable via Network tab. Documenting them in RECON.md is the intended purpose. |
| T-03-03 | Tampering | Function documentation accuracy | mitigate | All function specifications extracted from source code, not guessed. Cross-reference in Plan 05 verifies completeness. |
</threat_model>

<verification>
### Completeness Gates (for Plan 05)
- [ ] All 4 external API services have complete contract documentation
- [ ] Every endpoint has success AND error response documentation
- [ ] ≥55 functions documented with full template
- [ ] Function inventory coverage ≥90% against FEATURES.md
- [ ] Call graph is internally consistent (no dangling references)
</verification>

<success_criteria>
## Plan-Level Success
1. RECON.md §4 contains complete API contracts for all 4 external services (~250+ lines)
2. RECON.md §5 contains complete function documentation for ≥55 functions (~500+ lines)
3. Every API endpoint documented with success + error schemas (not just success)
4. Every function documented with signature + params + return + side effects + call graph
5. CORS proxy layer documented with URL rewriting rules
6. Function inventory coverage ≥90% against FEATURES.md
7. `api-har-archive.json` created and machine-readable
8. All claims extracted from source code (not guessed)
</success_criteria>

<output>
Create `.planning/phases/01-reconnaissance-extraction/01-03-SUMMARY.md` when done
</output>
