---
phase: 01-reconnaissance-extraction
plan: 05
type: execute
wave: 5
depends_on: [01-01, 01-02, 01-03, 01-04]
files_modified:
  - .planning/phases/01-reconnaissance-extraction/RECON.md
autonomous: true
requirements: [RECON-01, RECON-02, RECON-03, RECON-04, RECON-05]
user_setup: []

must_haves:
  truths:
    - "RECON.md is a single, coherent document of ≥1000 lines organized by technical layer per D-01"
    - "Every ROADMAP success criterion for Phase 1 is verified: ≥500 lines (should be ≥1000), localStorage schemas complete, API contracts with success+error, all 8 routes mapped, all ~60 functions documented"
    - "Cross-reference appendix (§7) maps every view to its DOM/API/State/Route/Function/CSS sections"
    - "Completeness audit against FEATURES.md inventory shows ≥95% coverage"
    - "The document is sufficient for a developer who has never seen the original to rebuild it"
    - "All 5 RECON requirements are traceably satisfied with section references"
  artifacts:
    - path: ".planning/phases/01-reconnaissance-extraction/RECON.md"
      provides: "Complete RECON.md — the authoritative specification for all downstream phases"
      contains: "All 7 layers + cross-reference appendix + completeness audit + metadata"
  key_links:
    - from: "RECON.md §7 Appendix"
      to: "RECON.md §1-6"
      via: "Every view cross-referenced with exact section references to all 6 layers"
    - from: "RECON.md completeness audit"
      to: "FEATURES.md inventory"
      via: "Coverage percentage per module"
    - from: "RECON.md requirement traceability"
      to: "REQUIREMENTS.md RECON-01 through RECON-05"
      via: "Each requirement mapped to RECON.md sections that satisfy it"

security_enforcement: false
---

<objective>
Compile all RECON.md sections produced by Plans 01-04 into a single coherent document, reorder sections per D-01 layer organization, build the cross-reference appendix, and validate completeness against all 5 RECON requirements and ROADMAP success criteria.

Purpose: Plans 01-04 each produced sections in extraction order (localStorage → DOM → Routes → APIs → Functions → CSS). This plan reorders them per D-01 (the layers as they should appear in the final document), builds the cross-reference appendix, and runs a rigorous completeness audit. The result is a polished, validated RECON.md ready for Phase 2 consumption.

Output: Final, validated RECON.md (≥1000 lines) with all 7 layers, cross-reference appendix, completeness audit, and requirement traceability.
</objective>

<execution_context>
@/home/matspectrum-ai/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/matspectrum-ai/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-reconnaissance-extraction/RECON.md
@.planning/phases/01-reconnaissance-extraction/data/raw-source.html
@.planning/phases/01-reconnaissance-extraction/01-CONTEXT.md
@.planning/phases/01-reconnaissance-extraction/01-RESEARCH.md
@.planning/research/FEATURES.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 01-05-01: Compile RECON.md — Reorder, Polish &amp; Finalize All Sections</name>
  <files>.planning/phases/01-reconnaissance-extraction/RECON.md</files>
  <read_first>
    - .planning/phases/01-reconnaissance-extraction/RECON.md (current state with all sections from Plans 01-04)
    - .planning/phases/01-reconnaissance-extraction/01-CONTEXT.md § Implementation Decisions (D-01: layer organization)
    - .planning/phases/01-reconnaissance-extraction/01-RESEARCH.md § Recommended RECON.md Structure (section ordering)
    - .planning/ROADMAP.md § Phase 1 Success Criteria
  </read_first>
  <action>
    Per D-01 (RECON.md organized by technical layer: DOM → APIs → State → Routes → Logic → CSS):

    **IMPORTANT:** Plans 01-04 extracted content in DEPENDENCY ORDER (localStorage → DOM → Routes → APIs → Functions → CSS). Per D-01, the FINAL document must be reordered to: DOM → APIs → State → Routes → Logic → CSS. The Appendix (§7) goes last.

    **Step 1 — Verify all sections exist:**
    Read the current RECON.md and confirm every section produced by Plans 01-04 is present:
    - §1: localStorage & State Schema (Plan 01)
    - §2: DOM Tree & Component Hierarchy (Plan 02)
    - §3: Rotas & Sistema de Navegação (Plan 02)
    - §4: Contratos de API (Plan 03)
    - §5: Funções de Lógica de Negócio (Plan 03)
    - §6: CSS / Design System (Plan 04)

    If any section is missing or incomplete, flag it — do NOT proceed with reorder until all content exists.

    **Step 2 — Reorder sections per D-01:**
    The D-01 order is: DOM → APIs → State → Routes → Logic → CSS.
    The RESEARCH.md recommended order (for internal structure) is: State → DOM → Routes → APIs → Logic → CSS.

    CONFLICT RESOLUTION: D-01 is a LOCKED DECISION — it takes precedence. However, note that "State" before "Routes" before "Logic" makes more sense for reader comprehension (data → navigation → behavior). The D-01 order is unusual but MUST be followed.

    Reorder the sections in the final RECON.md:
    ```markdown
    # RECON.md: Laboratório de BMs — Especificação de Engenharia Reversa

    ## 1. DOM Tree & Component Hierarchy
    ## 2. Contratos de API
    ## 3. localStorage & State Schema
    ## 4. Rotas & Sistema de Navegação
    ## 5. Funções de Lógica de Negócio
    ## 6. CSS / Design System
    ## 7. Apêndice: Referências Cruzadas por View
    ```

    When reordering, update ALL cross-reference annotations within sections. For example, if §5 (Logic) previously said "see §1.3 for state", and §1 is now §3, update the reference.

    **Step 3 — Update metadata block:**
    At the top of RECON.md, ensure the metadata block is complete:
    ```markdown
    **Target URL:** https://laboratoriodebms.netlify.app/
    **Extraction Date:** 2026-06-27
    **Extraction Method:** Source code analysis via webfetch (full HTML + JS source)
    **Tooling:** Chrome DevTools 149.x (webfetch-based source inspection)
    **Confidence:** HIGH (source-extracted, not guessed)
    **Original Source Lines:** ~2135 lines of inline vanilla JavaScript
    **Document Lines:** [count lines at end]
    **Phase Requirements Covered:** RECON-01 through RECON-05
    ```

    **Step 4 — Add requirement traceability table:**
    Add to the metadata block or as a preamble:
    ```markdown
    ### Requirement Traceability
    | Req ID | Description | RECON.md Section(s) | Status |
    |--------|-------------|-------------------|--------|
    | RECON-01 | DOM tree + vanilla JS confirmation | §1 (DOM Tree), §6 (CSS) | ✓ |
    | RECON-02 | API endpoints (success + error) | §2 (Contratos de API) | ✓ |
    | RECON-03 | localStorage schemas | §3 (localStorage & State) | ✓ |
    | RECON-04 | Route mapping (8 routes) | §4 (Rotas & Navegação) | ✓ |
    | RECON-05 | Business logic functions (~60) | §5 (Funções de Negócio) | ✓ |
    ```

    **Step 5 — Polish and consistency edit:**
    Read the full document for:
    - Consistent formatting: all API endpoints use same template, all functions use same template
    - Consistent terminology: "DOM", "API", "localStorage", "rota" used consistently
    - No placeholder text like "TODO", "TBD", "extract this", "[to be filled]"
    - No "probably", "maybe", "approximately", "about", "~" (approximation indicators)
    - No vague references: every cross-reference uses exact section numbers
    - Portuguese language consistency: document is in Portuguese (matching the original system)

    **Step 6 — Count total lines:**
    After finalization, count the total lines in RECON.md. The target is ≥1000 lines (ROADMAP says ≥500, CONTEXT.md says ~1000+). If below 800 lines, identify sections that need expansion. If between 800-1000, note for acceptance criteria.
  </action>
  <verify>
    <automated>
      # Verify all 7 sections exist in D-01 order
      grep -n "^## [1-7]\." .planning/phases/01-reconnaissance-extraction/RECON.md

      # Count total lines
      wc -l .planning/phases/01-reconnaissance-extraction/RECON.md

      # Verify no placeholder text remains
      grep -c "TODO\|TBD\|to be filled\|extract this\|placeholder" .planning/phases/01-reconnaissance-extraction/RECON.md

      # Verify no approximation language
      grep -c "probably\|maybe\|approximately\|aprox\.\|~ " .planning/phases/01-reconnaissance-extraction/RECON.md

      # Verify requirement traceability exists
      grep -c "RECON-0[1-5]" .planning/phases/01-reconnaissance-extraction/RECON.md
    </automated>
  </verify>
  <acceptance_criteria>
    - C1: RECON.md has all 7 sections in the D-01 order: DOM (§1), APIs (§2), State (§3), Routes (§4), Logic (§5), CSS (§6), Appendix (§7)
    - C2: All cross-reference annotations updated to reflect new section numbering (no broken references to old numbers)
    - C3: Metadata block complete with target URL, extraction date, method, confidence, line count
    - C4: Requirement traceability table present with all 5 RECON requirements mapped to sections
    - C5: Total line count ≥800 lines (target is ≥1000; 800 is minimum acceptable)
    - C6: Zero placeholder text ("TODO", "TBD", "to be filled", etc.)
    - C7: Zero approximation language ("probably", "maybe", "approximately", "about", "~")
    - C8: Portuguese consistency — document language matches original system
    - C9: Consistent formatting across all function/API/element documentation templates
    - C10: Document reads as a single coherent specification, not 4 separate documents glued together
  </acceptance_criteria>
  <done>RECON.md is a single coherent document with all 7 sections in D-01 order, complete metadata, requirement traceability, consistent formatting, and ≥800 lines.</done>
</task>

<task type="auto">
  <name>Task 01-05-02: Cross-Reference Appendix &amp; Completeness Validation</name>
  <files>.planning/phases/01-reconnaissance-extraction/RECON.md</files>
  <read_first>
    - .planning/phases/01-reconnaissance-extraction/RECON.md (finalized from Task 01-05-01)
    - .planning/research/FEATURES.md § Original System: Feature Inventory Summary
    - .planning/ROADMAP.md § Phase 1 Success Criteria
    - .planning/REQUIREMENTS.md § RECON-01 through RECON-05
  </read_first>
  <action>
    Per ROADMAP success criteria — validate RECON.md completeness against all 5 criteria:

    **Step 1 — Build the Cross-Reference Appendix (§7):**
    Create RECON.md §7 with this structure:
    ```markdown
    ## 7. Apêndice: Referências Cruzadas por View

    ### 7.1 Dashboard
    | Camada | Seção | Conteúdo Relevante |
    |--------|-------|-------------------|
    | DOM | §1.2 (← renumbered) | Dashboard view: hero card, 4 KPI stat cards, 6 quick-action cards |
    | APIs | Nenhuma | Dashboard não chama APIs externas |
    | State | §3.1 | Lê db.empresas.length, db.sites (filtrados por status) |
    | Routes | §4.1 | ROUTES[0]: path='dashboard' → VIEWS.dashboard |
    | Logic | §5.2 | VIEWS.dashboard(), statCard(), quickCard() |
    | CSS | §6.2.1, §6.2.3, §6.2.5 | .glass, .icon-cube, .nav-link.active |

    ### 7.2 Etapa 1
    ... [same for all 8 views]
    ```

    For EACH of the 8 views, create a cross-reference table that maps every layer to the specific RECON.md subsection. A developer building this view should be able to use this table as an index.

    **Step 2 — Verify ROADMAP Success Criterion 1 (≥500 lines):**
    ```bash
    wc -l .planning/phases/01-reconnaissance-extraction/RECON.md
    ```
    Document the actual line count. If ≥1000: "✓ Exceeds 500-line minimum". If 800-999: "✓ Meets 500-line minimum (target 1000)". If <500: "✗ FAIL" — expand sections.

    **Step 3 — Verify ROADMAP Success Criterion 2 (localStorage schemas):**
    Check that RECON.md §3 documents:
    - [ ] `lab_bms_db_v1` with complete JSON schema including ALL nested objects
    - [ ] `lab_bms_settings_v1` with complete JSON schema
    - [ ] Data types for every field
    - [ ] Conditional sub-objects annotated (empresas[].raw, sites[].deploymentId, sites[].phoneNumber)
    - [ ] When each key is created/updated
    Record: PASS/FAIL for each sub-check.

    **Step 4 — Verify ROADMAP Success Criterion 3 (API contracts with success + error):**
    Check that RECON.md §2 documents:
    - [ ] BrasilAPI CNPJ: success response schema + error response schema
    - [ ] Cloudflare Pages 5-step: each step has success + error
    - [ ] Cloudflare Account Detection: success + error
    - [ ] SMS24h: all endpoints have success + error
    - [ ] Request headers, response headers, status codes documented
    Record: PASS/FAIL for each sub-check.

    **Step 5 — Verify ROADMAP Success Criterion 4 (all 8 routes):**
    Check that RECON.md §4 documents:
    - [ ] ROUTES array with all 8 entries (dashboard, etapa1, etapa2, etapa3, banco, planilha, config, ajuda)
    - [ ] VIEWS registry with all 8 entries
    - [ ] go() function with header title/subtitle updates documented
    - [ ] CSS active states for nav-links documented
    - [ ] Navigation flow documented
    Record: PASS/FAIL for each sub-check.

    **Step 6 — Verify ROADMAP Success Criterion 5 (all ~60 functions):**
    Check that RECON.md §5 documents functions with:
    - [ ] ≥55 functions documented (target ~60)
    - [ ] Every function has: signature, parameters, return value
    - [ ] Every function has: state mutations documented (if applicable)
    - [ ] Every function has: side effects documented (if applicable)
    - [ ] Core/Infra functions (getDB, saveDB, go, toast, modal, etc.) → documented
    - [ ] Dashboard functions (VIEWS.dashboard, statCard, quickCard) → documented
    - [ ] Etapa 1 functions (VIEWS.etapa1, e1Buscar, e1Gerar, e1Publicar, etc.) → documented
    - [ ] Etapa 2 functions (VIEWS.etapa2, smsAPI, smsPolling, etc.) → documented
    - [ ] Etapa 3 functions (VIEWS.etapa3, carregarPDF, rerenderOverlays, etc.) → documented
    - [ ] Banco functions (VIEWS.banco, renderBanco, etc.) → documented
    - [ ] Planilha functions (VIEWS.planilha, renderPlanilha, etc.) → documented
    - [ ] Config functions (VIEWS.config, salvarConfig, etc.) → documented
    - [ ] Ajuda functions (VIEWS.ajuda, ajuda) → documented
    - [ ] Utils functions (fmtCNPJ, fmtMoney, fmtDate, etc.) → documented
    - [ ] Boot/Proxy functions (autoConectarTokens, instalarProxy) → documented
    Record: PASS/FAIL for each sub-check.

    **Step 7 — Cross-reference FEATURES.md inventory:**
    Compare RECON.md function list against FEATURES.md § Original System Inventory Summary:
    | Module | FEATURES.md Expected | RECON.md Found | Coverage |
    |--------|---------------------|----------------|----------|
    | Core/Infra | ~9 | [count] | [%] |
    | Dashboard | ~3 | [count] | [%] |
    | Etapa 1 | ~10 | [count] | [%] |
    | Etapa 2 | ~4 | [count] | [%] |
    | Etapa 3 | ~6 | [count] | [%] |
    | Banco | ~4 | [count] | [%] |
    | Planilha | ~5 | [count] | [%] |
    | Config | ~8 | [count] | [%] |
    | Ajuda | ~2 | [count] | [%] |
    | Site Gen | ~2 | [count] | [%] |
    | Boot/Proxy | ~2 | [count] | [%] |
    | Utils | ~7 | [count] | [%] |

    If any module has <100% coverage, flag it. If total coverage <90%, flag it as a completeness gap.

    **Step 8 — Validate each requirement (RECON-01 through RECON-05) is traceably satisfied:**
    For each RECON requirement:
    - RECON-01: Can a developer confirm the original uses vanilla JS and understand the full DOM tree? → Check §1 (DOM) + §6 (CSS)
    - RECON-02: Can a developer build API clients for all 4 services? → Check §2 (APIs)
    - RECON-03: Can a developer initialize localStorage with correct default data? → Check §3 (State)
    - RECON-04: Can a developer implement the SPA router with identical behavior? → Check §4 (Routes)
    - RECON-05: Can a developer replicate all ~60 business logic functions? → Check §5 (Logic)

    **Step 9 — Sufficiency test:**
    Ask: "If I were a developer who never saw the original, could I rebuild it from RECON.md alone?"
    - Can I build the router? (§4)
    - Can I build the persistence layer? (§3)
    - Can I build all 8 views? (§1 + §5)
    - Can I style everything identically? (§6)
    - Can I integrate all APIs? (§2)
    If any answer is "no", identify the gap and fix it.

    **Step 10 — Record completeness audit result:**
    Append to RECON.md metadata:
    ```markdown
    ### Completeness Audit Result
    - **Line Count:** [actual] / 1000 target ✓/✗
    - **localStorage Schema:** ✓/✗ (criterion 2)
    - **API Contracts (success+error):** ✓/✗ (criterion 3)
    - **Route Mapping:** ✓/✗ (criterion 4)
    - **Function Coverage:** [count]/60 — [%] ✓/✗ (criterion 5)
    - **FEATURES.md Cross-Reference:** [%] coverage
    - **Sufficiency Test:** ✓/✗ (could a developer rebuild from this?)
    - **Overall:** ✓/✗ ALL CRITERIA MET
    ```
  </action>
  <verify>
    <automated>
      # Verify cross-reference appendix exists with all 8 views
      grep -c "^### 7\." .planning/phases/01-reconnaissance-extraction/RECON.md

      # Verify completeness audit result recorded
      grep -c "Completeness Audit\|completeness" .planning/phases/01-reconnaissance-extraction/RECON.md

      # Final line count
      wc -l .planning/phases/01-reconnaissance-extraction/RECON.md

      # Verify no outstanding gaps
      grep -c "✗\|FAIL\|gap\|missing\|incomplete" .planning/phases/01-reconnaissance-extraction/RECON.md
    </automated>
  </verify>
  <acceptance_criteria>
    - C1: Cross-reference appendix (§7) has entries for ALL 8 views with references to every applicable layer
    - C2: Each view's cross-reference table has ≥4 rows (DOM, State, Routes, Logic at minimum)
    - C3: RECON.md line count verified and documented (target: ≥1000)
    - C4: All 5 ROADMAP success criteria individually verified as PASS or flagged as FAIL
    - C5: FEATURES.md cross-reference shows ≥90% function coverage
    - C6: All 5 RECON requirements traceably satisfied with section references
    - C7: Sufficiency test passed: a developer unfamiliar with the original could rebuild from RECON.md
    - C8: Completeness audit result recorded in RECON.md metadata
    - C9: Any FAIL results have explicit mitigation notes (e.g., "3 functions not found in source — possibly dead code or inline lambdas")
    - C10: No "✗ FAIL" without an explanation and remediation recommendation
  </acceptance_criteria>
  <done>RECON.md is complete, validated, and audit-passed: ≥1000 lines, all 5 RECON requirements satisfied, cross-reference appendix built, completeness audit recorded. Phase 01 deliverable is ready.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
N/A — Documentation-only phase.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | RECON.md completeness audit | mitigate | Automated grep checks verify section counts, requirement traceability, and absence of placeholder text. Manual sufficiency test confirms rebuildability. |
| T-05-02 | Repudiation | Extraction provenance | mitigate | Metadata block records extraction date, source URL, method, and confidence. All claims traceable to source code in `raw-source.html`. |
</threat_model>

<verification>
### Final Phase 01 Gate
- [ ] RECON.md ≥1000 lines of structured data
- [ ] All 5 ROADMAP success criteria verified PASS
- [ ] All 5 RECON requirements traceably satisfied
- [ ] Cross-reference appendix complete for all 8 views
- [ ] FEATURES.md coverage ≥90%
- [ ] Sufficiency test passed
- [ ] Completeness audit result recorded with PASS/FAIL for each criterion
</verification>

<success_criteria>
## Plan-Level Success
1. RECON.md is a single, coherent, polished document with all 7 sections in D-01 order
2. Line count ≥1000 (or ≥800 with documented reasons)
3. Cross-reference appendix (§7) covers all 8 views with layer references
4. Completeness audit shows ALL 5 ROADMAP criteria passing
5. All 5 RECON requirements traceable to specific sections
6. FEATURES.md function coverage ≥90%
7. Sufficiency test passed
8. RECON.md is ready for Phase 2 (Foundation) consumption — no Phase 1 rework needed
</success_criteria>

<output>
Create `.planning/phases/01-reconnaissance-extraction/01-05-SUMMARY.md` when done
</output>
