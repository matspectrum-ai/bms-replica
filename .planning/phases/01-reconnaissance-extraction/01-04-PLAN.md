---
phase: 01-reconnaissance-extraction
plan: 04
type: execute
wave: 4
depends_on: [01-03]
files_modified:
  - .planning/phases/01-reconnaissance-extraction/RECON.md
autonomous: true
requirements: [RECON-01]
user_setup: []

must_haves:
  truths:
    - "Every CSS custom property on :root is documented with exact value, purpose, and which components consume it"
    - "Every component CSS class (.glass, .grad-card, .btn-3d, .icon-cube, .pill, .nav-link, .step-card, .input, .stat-card, .quick-card) is documented with exact property values (not approximations)"
    - "All 8 btn-3d color variants, 5 icon-cube color variants, and 5 pill variants are documented with their exact gradient, shadow, and color values"
    - "All @keyframes animations (floaty, spinner, pulse-ring, etc.) are documented with exact keyframe percentages and property values"
    - "Responsive breakpoints (1024px sidebar collapse) are documented with exact media query syntax, affected elements, and behavioral changes"
    - "Tailwind CSS CDN version and configuration are documented"
  artifacts:
    - path: ".planning/phases/01-reconnaissance-extraction/RECON.md"
      provides: "Section 6: CSS / Design System — the complete visual specification for Phase 2 (Foundation) and baseline for Phase 5-6 (Rebrand)"
      contains: "CSS custom properties, component classes with all variants, animations/keyframes, responsive breakpoints, Tailwind CDN config"
  key_links:
    - from: "RECON.md §6.1 CSS custom properties"
      to: "RECON.md §2 (Plan 02) DOM elements"
      via: "Every custom property annotated with which components use it"
    - from: "RECON.md §6.2 Component classes"
      to: "RECON.md §5 (Plan 03) Widget factories"
      via: "Each CSS component class linked to the JS function that generates elements with that class"
    - from: "RECON.md §6.5 Responsive breakpoints"
      to: "RECON.md §2.1 Sidebar shell"
      via: "1024px breakpoint behavior maps to toggleSidebar() + backdrop overlay"

security_enforcement: false
---

<objective>
Extract and document the complete CSS/Design System of the original: all CSS custom properties, component classes with all color variants, @keyframes animations, responsive breakpoints, and Tailwind CDN configuration. Every value must be pixel-exact — no approximations.

Purpose: This is the visual specification. Phase 2 (Foundation) uses §6.1-6.2 to build the component library. Phase 5-6 (Rebrand) uses §6 as the baseline — every color, shadow, and gradient here gets swapped for the new palette. Approximate CSS produces cumulative 1-4px discrepancies across 50+ components (Pitfall 4), destroying A/B testing validity.

Output: RECON.md §6 (CSS / Design System, ~250+ lines).
</objective>

<execution_context>
@/home/matspectrum-ai/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/matspectrum-ai/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-reconnaissance-extraction/RECON.md
@.planning/phases/01-reconnaissance-extraction/data/raw-source.html
@.planning/phases/01-reconnaissance-extraction/01-RESEARCH.md
@.planning/research/FEATURES.md
@.planning/phases/01-reconnaissance-extraction/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 01-04-01: CSS Custom Properties &amp; Design Tokens Extraction</name>
  <files>.planning/phases/01-reconnaissance-extraction/RECON.md</files>
  <read_first>
    - .planning/phases/01-reconnaissance-extraction/RECON.md §2 (DOM elements — needed to know which classes exist)
    - .planning/phases/01-reconnaissance-extraction/data/raw-source.html
    - .planning/phases/01-reconnaissance-extraction/01-RESEARCH.md § Common Pitfalls — Pitfall 4: Approximate CSS Values
    - .planning/phases/01-reconnaissance-extraction/01-RESEARCH.md § Architecture Patterns — STEP 6: CSS / THEME
    - .planning/research/FEATURES.md § Differentiators (CSS details for 3D buttons, icon cubes, glassmorphism)
  </read_first>
  <action>
    Per D-02 (pixel-perfect CSS) and Pitfall 4 prevention (exact values, not approximations):

    ALL extraction is from `data/raw-source.html` — analyze the `<style>` blocks and inline `style=""` attributes in the HTML source.

    Prepare RECON.md §6 header: `## 6. CSS / Design System`.

    **Step 1 — Extract ALL `<style>` blocks from the source:**
    Search `raw-source.html` for `<style>` tags. The original is known to have inline CSS (not external stylesheets). Extract EVERYTHING between `<style>` and `</style>` tags. Also search for Tailwind CDN `<script>` tag (identified in Plan 01, Task 01-01-01).

    **Step 2 — Document `:root` CSS custom properties (§6.1):**
    Find the `:root { }` block in the CSS. Document EVERY custom property:
    ```markdown
    ### 6.1 CSS Custom Properties (:root)

    | Propriedade | Valor | Propósito | Consumido Por |
    |-------------|-------|-----------|--------------|
    | --bg | #0f172a | Fundo principal da aplicação | body, #content |
    | --card | rgba(30, 41, 59, 0.6) | Fundo de cards glass | .glass, .grad-card, .stat-card |
    | --border | rgba(148, 163, 184, 0.1) | Borda de elementos glass | .glass, .nav-link |
    | --text | #e2e8f0 | Cor de texto principal | body, todos os textos |
    | --text-secondary | #94a3b8 | Cor de texto secundário | subtitles, labels |
    | --primary | #6366f1 | Cor de destaque primária (navy blue) | .btn-3d, .icon-cube, .nav-link.active, .grad-text |
    | --primary-dark | #4f46e5 | Tom mais escuro do primário | .btn-3d (gradient), .icon-cube (dark end) |
    | --primary-light | #818cf8 | Tom mais claro do primário | .icon-cube (light start), .ring-glow |
    ...
    ```

    List ALL custom properties found. Expected count: 15-30 properties covering colors, spacing, shadows, border-radius, fonts, transitions.

    **Step 3 — Document color palette system:**
    Group custom properties by purpose:
    - Background colors (--bg, --bg-secondary, --bg-tertiary)
    - Surface/card colors (--card, --card-hover)
    - Text colors (--text, --text-secondary, --text-muted)
    - Accent/brand colors (--primary, --primary-dark, --primary-light, --accent, etc.)
    - Border colors (--border, --border-light)
    - Shadow colors (--shadow, --shadow-lg, --glow)
    - Status colors (--success/--danger for API pills)

    **Step 4 — Document font/typography tokens:**
    Find font-family declarations: body font, heading fonts, monospace fonts.
    Document Google Fonts imports (identified in Plan 01).
    Document font-size scale, font-weight scale, line-height defaults.

    **Step 5 — Document spacing/radius tokens:**
    Find any spacing scale custom properties or common values.
    Document border-radius scale (sm, md, lg, xl, full).

    **Step 6 — Cross-reference: where is each property used?**
    For each custom property, search the CSS to find which selectors use `var(--property)`. Annotate the "Consumido Por" column with specific class names. This is critical for Phase 5-6 rebrand — they need to know every place each color appears.
  </action>
  <verify>
    <automated>
      # Verify CSS section exists
      grep -c "^## 6\. CSS" .planning/phases/01-reconnaissance-extraction/RECON.md

      # Verify custom properties documented
      grep -c "^### 6\.1" .planning/phases/01-reconnaissance-extraction/RECON.md

      # Count documented properties (approximate — each table row with a property)
      grep -c "^| --" .planning/phases/01-reconnaissance-extraction/RECON.md
    </automated>
  </verify>
  <acceptance_criteria>
    - C1: ALL CSS custom properties from :root are documented (≥15 properties expected)
    - C2: Each property has: exact value, purpose description, and list of consuming components
    - C3: Color palette organized by purpose (background, surface, text, accent, border, shadow, status)
    - C4: Font/typography tokens documented: font families, scale, weights, line-height
    - C5: Spacing and border-radius tokens documented
    - C6: All values are EXACT (copied from source, not rounded or approximated)
    - C7: Google Fonts CDN URLs and exact font names documented
    - C8: "Consumido Por" column is populated — not "unknown" for any property
  </acceptance_criteria>
  <done>RECON.md §6.1 contains complete CSS custom properties documentation with exact values, purposes, and consumer cross-references.</done>
</task>

<task type="auto">
  <name>Task 01-04-02: Component Classes &amp; All Color Variants Extraction</name>
  <files>.planning/phases/01-reconnaissance-extraction/RECON.md</files>
  <read_first>
    - .planning/phases/01-reconnaissance-extraction/RECON.md §6.1 (custom properties — these are used by component classes)
    - .planning/phases/01-reconnaissance-extraction/RECON.md §2 (DOM elements — which elements use which classes)
    - .planning/phases/01-reconnaissance-extraction/data/raw-source.html
    - .planning/research/FEATURES.md § Differentiators (3D Button System, Icon Cube Design System)
  </read_first>
  <action>
    Per D-02 (pixel-perfect — every variant's exact gradient, shadow, color values):

    Document every reusable component CSS class in RECON.md §6.2. For each class, extract the FULL CSS rule set from the `<style>` blocks in `raw-source.html`.

    **Step 1 — Document `.glass` and `.grad-card` (§6.2.1):**
    ```markdown
    ### 6.2.1 Glassmorphism Cards

    #### .glass
    ```css
    .glass {
      background: rgba(30, 41, 59, 0.6);  /* --card */
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
      border: 1px solid rgba(148, 163, 184, 0.1);  /* --border */
      border-radius: 16px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
    }
    ```
    **Usage:** [list DOM elements from §2 that use .glass]

    #### .grad-card
    [same format — extract exact CSS]
    ```

    **Step 2 — Document `.btn-3d` button system (§6.2.2) — 8 color variants:**
    Find ALL `.btn-3d` rules and their color variant modifiers. Expected pattern:
    ```css
    .btn-3d {
      /* base 3D button styles */
      position: relative;
      padding: 13px 20px;
      border-radius: 8px;
      font-weight: 600;
      cursor: pointer;
      transition: transform 0.1s, box-shadow 0.1s;
      /* Two-layer shadow creating depth illusion */
      box-shadow: 0 4px 0 {dark-color}, 0 6px 20px rgba(0,0,0,0.3);
    }
    .btn-3d:active {
      transform: translateY(4px);
      box-shadow: 0 0 0 {dark-color}, 0 2px 10px rgba(0,0,0,0.3);
    }
    ```

    For EACH of the 8 color variants, document:
    ```markdown
    #### Variant 1: Primary (Indigo/Navy)
    - **Selector:** `.btn-3d.btn-primary` or `.btn-3d[data-variant="primary"]`
    - **Background:** `linear-gradient(160deg, #6366f1, #4f46e5 60%, #4338ca)`
    - **Bottom shadow:** `0 4px 0 #4338ca`
    - **Ambient shadow:** `0 6px 20px rgba(99, 102, 241, 0.4)`
    - **Active state:** `transform: translateY(4px)`, bottom shadow → `0 0 0`

    #### Variant 2: Success (Green)
    ... [same for all 8 variants]
    ```

    If the original uses Tailwind utility classes instead of CSS classes, document the exact utility class combinations that produce each variant.

    **Step 3 — Document `.icon-cube` design system (§6.2.3) — 5 color variants:**
    Find `.icon-cube` rules. Expected pattern:
    ```css
    .icon-cube {
      width: 48px; height: 48px;
      border-radius: 12px;
      display: flex; align-items: center; justify-content: center;
      /* Gradient background */
      /* Inner highlight shadow + bottom shadow */
    }
    ```

    For EACH of the 5 color variants, document:
    - Exact gradient: `linear-gradient(160deg, light, mid 60%, dark)`
    - Exact box-shadow layers: inner highlight + bottom shadow
    - Border (if any)
    - Icon color/size
    - Which views use each variant (from §2 DOM)

    **Step 4 — Document `.pill` badges (§6.2.4) — 5 variants:**
    Find `.pill` rules. Document for each variant:
    - Background color or gradient
    - Text color
    - Border (if any)
    - Border-radius
    - Padding
    - Font size/weight

    **Step 5 — Document `.nav-link` (§6.2.5):**
    Extract the full CSS for:
    - Default state: padding, border-radius, color, transition
    - Active state (`.nav-link.active`): background, color, border, shadow
    - Hover state: background change, transition
    - Icon styling within nav-link
    - Label styling within nav-link

    **Step 6 — Document `.step-card` (§6.2.6):**
    Wizard step containers. Document:
    - Default state
    - Disabled state (`.step-card.disabled` or similar) — opacity, pointer-events, visual dimming
    - Active/current state
    - Completed state (if any)

    **Step 7 — Document `.input`, `.stat-card`, `.quick-card`, `.neon`, `.ring-glow`, and any other component classes:**
    Search the CSS for ALL class selectors that represent reusable components. For each:
    - Full CSS rule set (exact, not summarized)
    - Which DOM elements use it (cross-reference §2)
    - Any variants/modifiers

    **Step 8 — Document any Tailwind `@apply` or `@layer` directives:**
    If the original uses Tailwind's `@apply` to compose utilities into component classes, document those exact directives.

    **Step 9 — State coverage check:**
    For each component class, verify ALL states are documented:
    - Default (mandatory)
    - Hover (if applicable)
    - Active/pressed (if applicable)
    - Disabled (if applicable)
    - Focus (if applicable, for accessibility)
    If the original CSS does NOT define a state, explicitly document "No [state] style defined — inherits or defaults to browser native."
  </action>
  <verify>
    <automated>
      # Verify component class sections exist
      grep -c "^#### \(\.\|Variant\)" .planning/phases/01-reconnaissance-extraction/RECON.md

      # Verify glass, btn-3d, icon-cube, pill classes all documented
      grep -c "\.glass\|\.grad-card\|\.btn-3d\|\.icon-cube\|\.pill\|\.nav-link\|\.step-card" .planning/phases/01-reconnaissance-extraction/RECON.md

      # Verify variant counts mentioned
      grep -c "variant\|variante\|Variante" .planning/phases/01-reconnaissance-extraction/RECON.md
    </automated>
  </verify>
  <acceptance_criteria>
    - C1: `.glass` and `.grad-card` fully documented with exact CSS properties and browser prefixes
    - C2: `.btn-3d` system fully documented: base class + ALL 8 color variants with exact gradients, shadows, and active-state transforms
    - C3: `.icon-cube` system fully documented: base class + ALL 5 color variants with exact gradients and multi-layer shadows
    - C4: `.pill` system fully documented: ALL 5 variants with exact background, text color, border-radius, padding
    - C5: `.nav-link` fully documented: default + active + hover states with exact properties
    - C6: `.step-card` fully documented: default + disabled + active states with exact properties
    - C7: Any additional component classes (.input, .stat-card, .quick-card, .neon, .ring-glow) documented with full CSS
    - C8: Every component class documents all applicable states (default/hover/active/disabled/focus)
    - C9: All gradient values are EXACT (e.g., `linear-gradient(160deg, #4f46e5, #3730a3 60%, #312e81)`)
    - C10: All shadow values are EXACT including ALL layers (e.g., `box-shadow: 0 4px 0 #4338ca, 0 6px 20px rgba(0,0,0,0.3)`)
    - C11: Every component class cross-referenced to DOM elements in §2 that use it
    - C12: No "approximately" or "about" — all values copied from source
  </acceptance_criteria>
  <done>RECON.md §6.2 contains complete component class documentation: glass, btn-3d (8 variants), icon-cube (5 variants), pill (5 variants), nav-link, step-card, and all other reusable classes with exact pixel values and multi-layer properties.</done>
</task>

<task type="auto">
  <name>Task 01-04-03: Animations, Responsive Breakpoints &amp; Tailwind Configuration</name>
  <files>.planning/phases/01-reconnaissance-extraction/RECON.md</files>
  <read_first>
    - .planning/phases/01-reconnaissance-extraction/RECON.md §6.1-6.2 (custom properties and component classes)
    - .planning/phases/01-reconnaissance-extraction/RECON.md §2.1 (sidebar shell — responsive behavior target)
    - .planning/phases/01-reconnaissance-extraction/data/raw-source.html
    - .planning/research/FEATURES.md § Table Stakes (Responsive Design, Dark Theme rows)
  </read_first>
  <action>
    Per D-02 (pixel-perfect — including animations and breakpoints):

    **Step 1 — Extract ALL @keyframes animations (§6.4):**
    Search the CSS in `raw-source.html` for `@keyframes` rules. Expected animations per FEATURES.md:
    - `floaty` — floating/bobbing animation (likely on hero elements)
    - `spinner` — loading spinner rotation
    - `pulse-ring` — pulsing ring effect (likely on status indicators)

    For EACH @keyframes rule, document:
    ```markdown
    ### 6.4.1 @keyframes floaty
    ```css
    @keyframes floaty {
      0%   { transform: translateY(0px); }
      50%  { transform: translateY(-10px); }
      100% { transform: translateY(0px); }
    }
    ```
    - **Duration:** [animation-duration value]
    - **Timing:** [animation-timing-function value]
    - **Iteration:** [animation-iteration-count value]
    - **Applied to:** [list selectors/elements that use this animation]
    ```

    Also document any `transition` properties on components (these are not @keyframes but are animation-adjacent):
    - Transition on `.btn-3d`: `transform 0.1s, box-shadow 0.1s`
    - Transition on `.nav-link`: background-color, color transitions
    - Transition on `.glass`: hover lift effect?

    **Step 2 — Document responsive breakpoints (§6.5):**
    Search the CSS for `@media` queries. The critical breakpoint is 1024px (sidebar collapse). Find:
    ```css
    @media (max-width: 1024px) {
      /* sidebar collapse */
    }
    ```

    Document EVERY @media query found, including:
    - Breakpoint value and direction (min-width / max-width)
    - ALL CSS rules inside the query
    - Which elements are affected
    - Behavioral changes:
      - Sidebar: collapse → `transform: translateX(-100%)` or width → 0?
      - Backdrop overlay: appears when sidebar is open on mobile
      - Content: margin/padding adjustments?
      - Grid changes: columns reduce?
      - Font sizes: scale down?
      - Touch targets: increase size?

    Document the EXACT sidebar collapse mechanism in detail:
    ```markdown
    ### 6.5.1 Breakpoint: 1024px (Mobile Sidebar Collapse)
    **Media Query:** `@media (max-width: 1024px)`
    **Sidebar behavior:** [exact CSS transformations]
    **Backdrop:** [element, styles, z-index]
    **Content adjustment:** [margin-left/padding changes]
    **Toggle trigger:** toggleSidebar() function (button/hamburger menu)
    **Hamburger menu:** [if it exists — document its appearance and position]
    ```

    **Step 3 — Document Tailwind CDN configuration (§6.6):**
    If the Tailwind CDN `<script>` was found in Plan 01, document:
    - Exact CDN URL with version
    - Any `tailwind.config = { }` block (custom theme extension, colors, fonts, breakpoints)
    - Any `@tailwind` directives in the CSS
    - Any custom utility classes defined via Tailwind's `@layer utilities`

    If the original uses Tailwind Play CDN (v3) vs the newer v4 CDN, note the version difference — it affects available utility classes.

    **Step 4 — Document any additional CSS patterns not covered above:**
    Search for:
    - `@font-face` declarations (custom fonts beyond Google Fonts)
    - `::before` / `::after` pseudo-elements (decorative effects)
    - CSS `filter` properties (blur, brightness on glass elements)
    - `backdrop-filter` properties (glassmorphism blur)
    - `scrollbar` styling (custom scrollbar CSS)
    - `::selection` styling
    - Print styles (`@media print`)
    - Dark mode / color-scheme declarations

    **Step 5 — Document the complete CSS architecture:**
    Summarize RECON.md §6 in §6.7:
    ```markdown
    ### 6.7 CSS Architecture Summary
    - **Approach:** [inline `<style>` block / external file / Tailwind CDN + custom CSS]
    - **Custom properties:** [count] on :root
    - **Component classes:** [count] with [count] total variants
    - **Animations:** [count] @keyframes + [count] transitions
    - **Breakpoints:** [list all breakpoints]
    - **Total CSS size:** [approximate line count / KB]
    ```
  </action>
  <verify>
    <automated>
      # Verify animations documented
      grep -c "@keyframes\|keyframes\|floaty\|spinner\|pulse-ring" .planning/phases/01-reconnaissance-extraction/RECON.md

      # Verify responsive breakpoints documented
      grep -c "@media\|breakpoint\|1024px\|responsive" .planning/phases/01-reconnaissance-extraction/RECON.md

      # Verify Tailwind config documented
      grep -c "tailwind\|Tailwind\|cdn.tailwindcss" .planning/phases/01-reconnaissance-extraction/RECON.md

      # Verify CSS architecture summary exists
      grep -c "^### 6\.7" .planning/phases/01-reconnaissance-extraction/RECON.md
    </automated>
  </verify>
  <acceptance_criteria>
    - C1: ALL @keyframes animations documented with exact keyframe percentages and property values
    - C2: Every animation includes: duration, timing function, iteration count, and applied-to selector list
    - C3: ALL transition properties on interactive elements documented (btn-3d, nav-link, glass, etc.)
    - C4: All @media queries documented with exact breakpoint values and affected element list
    - C5: 1024px mobile breakpoint documented in detail: sidebar collapse mechanism, backdrop overlay, content adjustment, hamburger menu trigger
    - C6: Tailwind CDN URL and version documented with any custom config
    - C7: Any pseudo-elements, filters, backdrop-filters, scrollbar styling, or print styles documented
    - C8: CSS Architecture Summary (§6.7) provides quantitative overview
    - C9: All values EXACT — keyframe percentages, pixel values, timing values copied from source
  </acceptance_criteria>
  <done>RECON.md §6.4-6.7 complete: all animations documented, responsive breakpoints detailed, Tailwind CDN config captured, CSS architecture summarized. RECON.md §6 is now complete (~250+ lines).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
N/A — Documentation-only phase.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | CSS value accuracy in RECON.md | mitigate | All CSS values extracted from source code, not visually approximated. Exact hex codes, pixel values, and gradient definitions copied directly. |
</threat_model>

<verification>
### Pixel-Perfect Gate (for Plan 05)
- [ ] Every CSS value is exact (no "~", "≈", "about", "approximately")
- [ ] Every multi-layer property (box-shadow, gradient) has ALL layers documented
- [ ] Every color has a hex code, not a name ("#6366f1", not "navy blue")
- [ ] All 8 btn-3d variants documented
- [ ] All 5 icon-cube variants documented
- [ ] All 5 pill variants documented
</verification>

<success_criteria>
## Plan-Level Success
1. RECON.md §6 exists with ≥200 lines covering all CSS layers
2. All CSS custom properties (≥15) documented with exact values and consumers
3. All component classes documented with all color variants (8 btn-3d + 5 icon-cube + 5 pill minimum)
4. Every component class documents all applicable states (default, hover, active, disabled)
5. All @keyframes animations documented with exact keyframes
6. All @media queries documented with exact breakpoints and behavioral changes
7. 1024px responsive breakpoint detailed with sidebar collapse mechanism
8. Tailwind CDN version and config documented
9. Zero approximations — every value is exact and source-extracted
</success_criteria>

<output>
Create `.planning/phases/01-reconnaissance-extraction/01-04-SUMMARY.md` when done
</output>
